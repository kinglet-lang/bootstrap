static_library("checker") {
  sources = [
    "type_checker.cc",
    "type_checker.h",
  ]

  deps = [
    "//src/ast",
    "//src/ir",
    "//src/module",
    "//src/types",
  ]

  include_dirs = [
    "//src",
  ]
}
