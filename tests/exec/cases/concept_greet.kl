// RUN: run
// EXPECT-STDOUT: 2

using io;

concept Printable<T> {
  string to_string(T value);
}

string to_string(int v) {
  return "ab";
}

string greet(Printable item) {
  return to_string(item);
}

int main() {
  string s = greet(42);
  io::out.line(s.len());
  return 0;
}
