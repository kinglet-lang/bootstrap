// RUN: run
using io;

int add(int a, int b) { return a + b; }
float add(float a, float b) { return a + b; }
string add(string a, string b) { return a + b; }

int main() {
  io::out.line(add(10, 20));
  io::out.line(add(1.5, 2.5));
  io::out.line(add("he", "llo"));
  return 0;
}
