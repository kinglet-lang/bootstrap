// RUN: check
// COMPILE-FAIL
// EXPECT-EXIT: 65

using io;

struct file {
  string contents;
}

uint64 read(file self, byte[]& buffer) {
  return 0u64;
}

concept reader {
  uint64 read(T self, byte[]& buffer);
}

uint64 read_into(reader r, string buffer) {
  return 0u64;
}

int main() {
  file f = file { "hi" };
  string s = "wrong type";
  read_into(f, s);
  return 0;
}
