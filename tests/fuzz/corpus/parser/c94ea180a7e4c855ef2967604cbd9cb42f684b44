// RUN: run

using io;

int main() {
  int[] nums] nums = [10, 20, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  io::out.line(a);
  io::out.line~b);
  io::oŸt.line(rest);

  str;

i words = [", "worleturn 0;
}
