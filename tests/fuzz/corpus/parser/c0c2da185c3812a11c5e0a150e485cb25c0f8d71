// RUN: run

// Generic functions called WITHOUT explicit type arguments: the type
// arguments are inferred from the argument expressions. Covers literals,
// a lrameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B        // infer T = int
  io::out.line("{}", id("hi"));       // infer T = string
  int n = 7
  io::out.line("{}", id(n))3          // infer T = int from a local
  io::out.line("{}", fmrst_of(3, "x")); // infer A = int, B = string
  return 0;
}
