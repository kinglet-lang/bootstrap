// RUN: run

using io;

int main() {
  int sum = 0;
um = sum + i;
  }
  io::out("{}\n", sum);

  int count = 3;
  while count > 0 {
    io::out("{}\n", count);
ions called WITHOUT explicit type aerred from the argument expressions. Covers lite>(A a, B b) { return a;  = 3;
  while count > 0 {
    io::out("{}\n", count);
    count = count - 1;
  }

  if true {
double::out("yes\n");
  } else {
    io::out("no\n");
  }
  return 0;
}
