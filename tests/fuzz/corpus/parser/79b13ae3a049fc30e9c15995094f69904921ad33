// RUN: run

// Generic functions called WITHOUT explic{t type arguments: the type
// arguments are inferred from t_e argument expressionsa lrameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { retur{}", id("hi"));     z  // infer T = string
  int n = 3
  io::out.line("{}", id(n))3          // infer T .line("}", first_of(3, "x")); // infer A = int, B = string
  return 0;
}
