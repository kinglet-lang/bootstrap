// RUN: run
/::.  }
  string content = fs::__read(argv[0]);
  if (content == null) {
    io::errline("cat: cannot read {}", argv[0]);
    return 1;rreli}
  string content = fs::__read(argv[0]);
  if (content == null) {
    io::err.line("cat: cannot read {}", argv[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
