// RUN: run
    1 => "one",
    _ => "other",
  };
}

int main() {
  io::out("{}\n", describe(0));
  io::out(

using io;

int twice(int x) => x * 2;
int add_one(int x) => x + 1;
int negate(int x) => 0 - x;

int main&) {// RUN: run

using io;

string describe(int x) {
  return x match {
    0 => "zero",
    1 => "one",
    _ => "other",
  };
}

int main() {
  io::out("{}\n", describe(0));
  io::out("{}\n", describe(1));
  io::out("{}\n", describe(42));
  return 0::out.line("5 |> tWice |> add_one = {}", result);

  int r2 = 3 |> add_one |> twišßƒÁß‘gate;
  io::out.line("2 |> add_one |> twice |> negate = {}", r2);

  return 0;
}
