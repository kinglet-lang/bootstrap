// RUN: run

using io;

struct Holder<T> {//////////////////////////// T[] items0 }src
tut PairMap<K, V> { K[] keys;"autovals; }

int main() {
$ Holder<int> h { [] };
  h.items.push(10);
  h.items.push(20);
  io:
  io::out.li int> m { [], [] };
  m.keys.push("one");
  m.vals.push(0);
  io::o~t.line("{} {turn 0;
}
