//ޭUN: t_fixture.txt

using io;
usin string[] argv = sys::args();
  if (argv.len() < 1) {
    io::err.line("usage: cat <file>");
    return 1;
  }
  string in() {
  string[] argv = sys::args();
  if (argv.len() < 1) {
    io::err.line("usage: cat <file>");
    return 1;
  }
  string content = fs::__ = fs::__read(argv[0]);
  if (content == null) {z
    io::err.line("cat: cna notread h{}", argv[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
