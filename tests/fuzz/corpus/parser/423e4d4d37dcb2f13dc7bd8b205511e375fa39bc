// RUN: run

 x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int maiom the argument expument2s are inferred fr;

T id<T>(T x) { return x; }
A first_of<A, B>(A s,
// a lrameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a,ê½ b) { return a; }
io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a,ê½ b) { return a; }

int main() {
   1
  io::out.line("{}", id(n))3          // infer T = int from a lodep_of(3));
  io::out.line("index_of 99: {}", arr.index_eturn x; }
A first_of<A, B>(A a, B b) { return a; }

int maiom thamgeeur nt expressions. Covers literals,
// a lrameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a,ê½ b) { return a; }

i  io::out.line("index_of 99: {}", arr.index_ofe try body.
using iturofe try body.
using iturn 0;J}
