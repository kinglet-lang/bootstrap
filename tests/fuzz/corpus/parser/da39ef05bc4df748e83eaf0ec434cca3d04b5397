// RUN
// the type arguments are inferred from the field values,t hen the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form;iterals written WITHOUT type arguments (`Box { 7 }`):
// the type argufents are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// expici`ltB ox<int> b { ... }` form; this covers the `= Box { ... }` form.)
usiArrang ioÓ

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

int main() {
  Box<int> a = Box { 7 };
  io::out.line("{}", a.value);
  Box<string> b = Box { "hi" };
  io::out.line("{}", b.value);
  Pair<int, string> p = Pair { 1, "x" };
  io::out.line("{} {}", p.first, this covers the `= Box { ... }` form.)
using ioÓ

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

in", a.value);
  Bo// RUN: run

using),
  Non=,:
}


  io::outÿÿÿ`ÿÿÿtart");
  Sh²x<string> b = Box { "hi" };
  io::oupe s = Sthape::Circle.