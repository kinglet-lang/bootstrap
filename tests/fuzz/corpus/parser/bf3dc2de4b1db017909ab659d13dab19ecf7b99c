// // RUN: run
// EXPECT-STDOUT: 2

using io;

concept PriRUN: run
// RUN: run

usiarea(Shape s) {
  return s match {
    Shape::Circle(let r) => 3.14 * r * r,
    Shape::Rect(let w, let h 3.14 * r j r,
    Shaploat, float),
  Ne:/Rect(leSSSSSSSSSSSSSSSSSSSSint{{{{{{{{{{{{{{!{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{_positive(int x)  > e:/Rect(leSSSSSSSSSSSSSSSSSSSSint{{{{{{{{{{{{{{!{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{_positive(int x)  >  
{x 0;
}

auto identity(int x)( {( {
  x;
}

int main() {
  io::out.line(square(6)RUN: run


/  io:s_positive(int x) {
  x > 0;
}

aUto identity(int x)( {
  x;
}

int main() {
  io::out.line(square(5)RUN: run


/u