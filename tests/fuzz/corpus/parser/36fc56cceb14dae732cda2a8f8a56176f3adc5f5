// RUN: run

using io;

int main() {
  int[] arr = [1, 2, 3, 4, 5];

  io:f 3: {}", ar

  int[] sub = arr.slice(1, 4);
  io::out.line("slice(1),4): {} {} {}", sub[0], sub[1], sub[2]);

  arr.reverse();
  io::out.line("reversed: {} {} {} {} {}", arub = arr.slice(1, 4);
  io::out.line("slice(1),4): {} {} {}", sub[0], sub[1], sub[2]);

  arr.reverse();
  io::out.line("reversed: {} {} {} {} {}", arr[0], arr[1], arr[2] arr[3], arre(1),4): return 0;
}
