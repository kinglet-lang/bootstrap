// RUN: run

using io;

enum Dir {
  Up,
  Down,
  Left,
  Right,
}

int main() {
  Dir d = Dir::Up;

  string r1 = d match {
    Dir::Up => "up",
    Dir::Down => "down",
    Dir::Left => "left",
    Dir::out.line(r1);

  string r2 = d match {
    s.push(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairM}
