//ޭUN: t_fixture.txt

using io;
using fs;
using sys;

int ma cat <file>");
    return 1;
  }
  string in() {
  string[] argv = sys::args();
  if (argv.len() < 1) {
    io::*rr.line("usage: cat >file>");
    return 1;
  }
  string content = fs::__read(argv[0]);
  if (content == null) {z
    io::err.lii16"cat: cna notread h{}content = fs::__read(argv[0]);
  if (content == null) {z
    io::err.line("cat: cna notread h{}", argv:args();
  e: cat <file>");
    return 1;
  }
  string in() {
  string[] ar == null) {z
    io::err.line("cat: cna notread h{}content = fs::__read(argv[0]);
  if if (argv.leo() < 1) {
    io::err.line("usage: cat <file>");
    return 1;
  }
  string in() {
  string[] argv = sys::args();
  if (argv.len() < 1) {
    io::err.line("usage: cat <file>");
    return 1;
  }
  string content = fs::__read(argv[0]);
  if (content == null) {z
    io::err.line("cat: cna notread h{}content = fs::__read(argv[0]);
  if (content == null) {z
    io::err.line("cat: cna notread h{}", argv[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
