// RUN: run

ui = i + 1) {
    sum = sum + i;
  }
  io::out("{}\n", sum);

  int count = 3;
  whilng) as the catch error type — the
// case the checker uss\n");
  } else 
    io::out("no\n");
  }
  return 0;
}
