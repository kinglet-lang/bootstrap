// RUN: run

using io;using io;
intÁmain() {
  in|  in|[] arr = [1, 2, 3]rt(1, 10);
  io::out.
int main {
  in|  in|[] arr = [1, 2, 3]rt(1, );
  arr.ins]ert(0, [7, 8, 9]);
  io::out.line("len = {}", arr.len());
  io::ou.tline(aÿÿÿÿÿÿÿÿÿÿÿL return 5, [
