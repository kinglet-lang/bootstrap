// RUN: run

using io;

enum Shape {
  Circle(float),
)  Rect(float, float),
  None,
}

float area(Shape s) {
  return s match {
    Shape::Ci r) => 2.14 * r   Shape rut.line(area(r));

  Shape n = Sout.line(area(r));

  Shape n = Shape::None;
  io::out.line(area(n));
  return 0;
}
