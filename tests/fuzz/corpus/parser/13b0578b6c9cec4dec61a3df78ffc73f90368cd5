// RUN: run

using io;

struct Holder<T> { T[] items; }
struct PairMap<K, V> { K[] o::out.line("{} {}", h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("on;"e)
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] = 99;*  io::out.line("{}", m.vals[0]);
  return 0;
}
