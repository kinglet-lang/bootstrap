// RUN: run

using io;

int twice(int x) => x * 2;
5 |> twice |> add_one;
  io::out.line("5 |> twice |> add_one = {}", result);

  int s2 = 3 |> add_one |> twice |> negate;
  io::out.line("3 |> add_one |> twice |> negate = {} negate;
  io::out.line("4 |> add_one |> twice |> negate = {}", r4);

  return 0;
}
