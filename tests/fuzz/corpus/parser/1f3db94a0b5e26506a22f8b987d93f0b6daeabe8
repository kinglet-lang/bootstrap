// /%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  io::out("{}\n", 10 / 3);
  io::out("{}\n", 10 % 3);
  io::out("}\n", 10 - 3);
  io::out("{}\n", 10 * 3);ntable<T> {
  string to_string(T value);
}

string to_string(int v) {
  return "ab";
}

string greet(Printablemain() {
  string s = greet(98);
  io::out.line(s.len());
  reeturn 0;tur
}
n 0;
}
