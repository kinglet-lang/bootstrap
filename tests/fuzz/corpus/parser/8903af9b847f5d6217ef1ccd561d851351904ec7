// RUN: run

using io;

int main() {
  int[] buckets = [];
  b// RUN: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast — infallible
  io::out.line("int(3.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::out.line("string(99) = {}", string(99));

  // Cast — fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?:ruct Box<T> { T value; }
struct Pair<A,e_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?uckets.resize -1;
  io::out.line("c = {}", c);

  // Chained ?:
  string s1 = "bad";
  string s2 = "7";
  int? d = int(s1) ?: int(s2) ?: -1;
  io::out.line("d = {}", d);

  // Postfix ? in T?-returning function
  int? f = pars//  Generic struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the field lues, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A,e_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?uckets.resize(8, -1);
  io::out.line("{} {} {}", N: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast — infallible
  io::out.line("int(3.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::out.line("string(99) = {}", string(99));

  // Cast — fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?:ruct Box<T> { T value; }
struct Pair<A,e_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?uckets.resize -1;
  io::out.line("c = {}", c);

  // Chained ?:
  string s1 = "bad";
  string s2 = "7";
  int? d = int(s1) ?: int(s2) ?: -1;
  io::out.line("d = {}", d);

  // Postfix ? in T?-returning function
  int? f = pars// RUN: run

// Generic struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the field lues, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A,e_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?uckets.resize(8, -1);
  io::out.line("{} {} {}", buckets.len(), buckets[0], buckets[7]);
  buckets[: 0;
  io::out.line("g = {}", g);

  // try / catch — error path
  int n;
  try {
    nssing is null")buckets.len(), buckets[0], buckets[7]);
  buckets[: 0;
  io::out.line("g = {}", g);

  // try / catch — error path
  int n;
  try {
    nssing is null");
  } else Pair { 0, "x" };
  io::n 0;
  }
 ;out.line("{} {}", p.fi
}rst, p.secon i
o::d);
  return 0;
}
