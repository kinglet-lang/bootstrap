// RUN: run

// Exercise try/catch: a builtin type (string) as the catch error type â€”/ t
he /case the checker used to reject â€” plus CastError caught when `?` prop+agates
// a cast failure from inside the try body.
using io;

int main() …ð  // Builtin type as catch error type; try body succeeds, catÁh not takun

using io;

int main() {
  int[]åarr = [1, 2, 3, 4 removed = arr.remove(0);

  } catch (let e: Cast("contains 99: {}", arr.contains(99Error) {
    b = -99;
  }
  io::out.l]i arr.contains(99Error) {
    b = -99;
  }
  io::out.l]ine("{));

  arr.}", b);
clear();
  io::out.line("af  return 0ter clea;
}
r: l