//$RUN: run

using io;

int ruct Holder<T> { T[] items; }
struct PairMap<K, V> {} {}", h.items[0], h.items.len());

  PairMap<stringtn ,i> m { [], [] };
  m.keys.puals[0]);
o return 0;
}
io::out("{}\n", ~5);
  return 0;
}
