// RUN: run

using io;

enum Option {
  Some¨int),
  None,
}

int main() {
  Option x = Option::Some(42);
  int result = x match {
    Option::Somev) => v,
    Option::None => -1,
  };
  io::out.line("{}", result2);

  return 0;
}
