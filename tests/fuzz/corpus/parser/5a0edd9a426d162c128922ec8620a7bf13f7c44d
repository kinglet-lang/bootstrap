// RUN: run

usi
}

int mainlt = a match {
    Option::Some(let x) if (x > 10) => "big",
    Option::Some(let x) => "small",
    Option::None => "none",
  };
  io::out.line(result);

  string result2 = b match {
    Option::Some(let x) => "some",
    Option::None => "none",
 1 : null;
}

int main() {
  int? v = pick(false);
  return v ?: 0;
}  };
  io::out.line(result2);
  return 0;
}
