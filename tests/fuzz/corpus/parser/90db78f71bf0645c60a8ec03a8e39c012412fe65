// RUN: run

// Generic functions called WITHOUT explicit type arguments: the type
// arguments are inferred from the argument expressions. Covers literals,
led WITHOUT explicit type arguments: the type
// arguments are inferred fr;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int maiom the argument expressions. Covers literals,
// a lrameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int main() {
   7
  io::out.line("{}", id(n))3          // infer T = int from a lodex_of(3));
  io::out.line("index_of 99: {}", arr.index_ofe try body.
using iturn 0;
}
