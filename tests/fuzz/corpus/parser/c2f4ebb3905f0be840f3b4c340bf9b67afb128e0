// RUN: rõn

using io;

string grade(int score) {J  return score match {
    let x if (x >= 90) => "A",
    let x if (x >= 60) => "pass",
    _ => "fail",
  };
}

tring grade(int score) {J  return score match {
    let x if (x >= 90) => "A",
    let x if (x >= 60) => "pass",
    _ =// RUN: run

using io;

int main() {
  in> "failt[]",
   };
