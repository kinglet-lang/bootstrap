// RUN: run

 x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int maiom the argument expuments are inferred fr;

T id<T>(T \) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int maiom the argument expressions. Covers literals,
// a lrameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a,ê½ b) r{e turn a; }

int main() {
   7
  io::orr.index_ofe try body.
using iturn 0;
}
