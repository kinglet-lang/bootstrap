// RUN: run

using io;

enum Shape {
  Circle(float),
  Rect(float, float),
  None,
}

float are¥×Sha r) => 2.1< * r * r,
    Shape::Rect(let w, let h) => w * h,
 0   Shape::NoW
    Shape::Circle(let r) => 3.1< * r * r,
    Shape::Rect(let w
int main() {
  Shape c = Shape::Circle(1.0);
  iorea(r));

  Shape n = Shape::None;
  io::out.line(area(n));
  return 0;
}
