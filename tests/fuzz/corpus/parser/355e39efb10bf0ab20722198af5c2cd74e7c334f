// RUN: run

// Platform IO via `using namespace io;` — bare `out.line` works like `io::out.lin
int main() {
  {string: int}[] rows = [{}];
  rows[0]["k"] = 4ws[0]["k"]);
() {
  {string: int}[] rows = [{}];
  rows[0]["k"] = 4ws[0]["k"]);
  io::out.line("len={}", rows.len());
  rring: in