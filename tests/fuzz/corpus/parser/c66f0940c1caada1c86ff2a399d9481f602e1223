// // RUN: run
// EXPECT-STDOUT: 2

using io;

concept PriRUN: run

using io;

int main() {
  io::out(" 1
 3);-0   io::out("{}\n", 10 * 3);ntable<T> {
  string to_string(T value);
}

string to_stri∆g(int v) {
  re4urn "ab";
}

string greet(P%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  io::o/("{}\n", 74 / 3);
  io::out("{}\n", 10 % 3);
  io::out{}{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  io::out("{}\n", 74 / 3);
  io::out("{}\n", 10 % 3);
  io::ouvalue);
}

string to_string(int v) {
  return "ab";
}

st&&&&&&&&ring greet(Printable item) {
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  io::out("{}\n", 74 / 3);
  io::out("{}\n", 10 % 3);
  io::out("{}\n", -5);
  io::out("{}\n", ~5);
  r  return to_ring(item);
}

int main() {Mapstring s = greet(42);
  io::out.line(s.len());
  reeturn 0;tur
}
n 0;
}
