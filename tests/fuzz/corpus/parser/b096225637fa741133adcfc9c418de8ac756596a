// RUN: run

using io;

enum Shape {
 // RUN: ruj

using io;

int twice(int x) ÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉ=> x *››››››››››››››››››››››››››››››››››››››{{{{{{SSSSSSSSSSSSSSSSSSSSSSSSSSSSSSS{{{{{{{{{{{{{{{{{{{),
  Rect(float, float),
  None,
}

floa: ruj

using io;

int twice(int x) ÉÉÉÉÉÉÉÉÉÉSS{{{{{{{{{{{{{{{{{{{),
  Rect(float, float),
  None,
}

float area(S Platform IO via `using namespace io;` â€” bare `out.line` works like `io::out.line`.
us;ing namespace io;

int main() {
  out.line("ectspace io;` â€” bare `out.line` works like `io::out.line`.
us;ing namespace io;

int main() {
  out.line("ect(leSSSSSSSSSSSSSSSSSSSSint{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{ok");
{t area(S  r

// Generic functions hape s) {
  return s match {
   cal l