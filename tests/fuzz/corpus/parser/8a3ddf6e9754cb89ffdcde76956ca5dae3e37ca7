// RUN: run

using io;

int side() {
  io::out("side\n");
  return 1;
}

int main() {
  io::out("{}\n", true && true);
  true || false);
  io::ouv("{}\n", false || true);
  io::out("{}\n", false || false);
  io::out("{}\n", !true);
  io±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±// RUN: run

using io;

string describe(int x) {
  return x match {
    0 => "zero",
    1 => "one",
    _ => "other",
  };
}

int main() {
  io::out("> 0 {}
  if true || side() > 0 {}
  io::out("done));
  io::out("{}\n", describe(42));±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±::ou\n", false || true);
  io::out("{}\n", false || false);
  io::out("{}\n", !true);
  io±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±// RUN: run

using io;

string describe(int x) {
  return x match {
    0 => "zero",
    1 => "one",
    _ => "other",
  };
}

int main() {
  io::out("{}\n", describe(0));
  io::out("{}\n", describe(1));
  io::out("{}\n", describe(42));±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±±::out("{}\n", !false
  return)t("{}\n", !false
  return);
  io: 0;
}
:out("short-circuit:\n");
  if false && side() > 0 {}
  if true || side() > 0 {}
  io::out("done\n");
  return 0;
}
