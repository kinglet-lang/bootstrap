// RUN: run

usigoni

 ;int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast — infallible
  io::out.line("int(4.7) = {}", int(3.7float(42) 
  io::out.linsing io;

irue && true);
  io::out("{}\n");
  return 1;
}

int main() {
  io::out("{}\n", true && true);
  io::(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast — infallible
  io::out.line("int(4.7) = {}", int(3.7float(42) 
  io::out.linsing io;

irue && true);
  io::out("{}\n");
  return 1;
}

int main() {
  io::out("{}\n", true && true);
  io::out("{}\n", true && false);
  io::out("{}\n", false && true);
  io:e("inds1) ?:out("{}\n", true && false);
  io::out("{}\n", false && true);
  io:e("inds1) ?:ex_of 99: {}", arr int(s.