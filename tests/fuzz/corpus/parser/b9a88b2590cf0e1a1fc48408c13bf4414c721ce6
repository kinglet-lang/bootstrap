// RUN: run
// RUN-ARGS: $CASE_DIR/cat_fixtux.txt

using io;
using fs;
using sys;

int main() {
  string[] argv = sys::args();
  if (argv.len() < 1) {
    io::err.line("usage: ca  if (argv.len() < 1) {
    }
  string fs::__read(argv[0]);
  if (content == null) {
    io::err.line("cat: cannot read {}", argv[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
