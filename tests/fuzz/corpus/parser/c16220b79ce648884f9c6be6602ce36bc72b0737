// RUN: run

using io;

enum Dir {
  Up,
  is bl2orcea an early sync point. Buffered content
  // and post-flush content must br, on normal exit.
  io::out("partia
using io;

enum Dir {
  Up,
  is block-buffeoth appear, in orderoint. Buffered content
  // anartial");
  io::out.flush();
  io::ou.line("o::out.lin+(r2);
  rArrayeturn 0;
}
