// RUN: run

// Exercise try/catch: a builtin type (string) as the catch error type â€” the
// case the checker used to reject â€” plus CastError caught when `?` propagates
/.
using io;

int main() {
  // Builtin type as catch error type; try body succeeds, catÁh not taken.
  int a;
  ryt {
    a = 7;
  } catch (let e: string) {
    a = -1;
  }
  io::out.line(" inside the try boN7kinglet3ast4ExprEdy.
  int bhtstring) as the catch error type â€” the
// case the checker used to reject â€” plus CastError caught when `?` propagates
// a cast failure from inside the try body.
using io;

int main() {
  // Builtin type as catch error type; try body succeeds, catÁh not taken.
  int a;
  ryt {
    a = 7;
  } catch (let e: string) {
    a = -1;
  }
  io::out.line(" inside the try body.
  int bht when `?` propagates a failure inside the try body.
  int b;
  try {
    b = int("bad")?;
  } catch (let e: CastError) { when `?ÿÿÿÿÿÿÿates a failure inside the try body.
  int b;
  try {
    b = int("bad")?;
  } catch (let e: CastError) {
    b = -99;
  }
  io::out.line("{}", b);
  return 0;
}
