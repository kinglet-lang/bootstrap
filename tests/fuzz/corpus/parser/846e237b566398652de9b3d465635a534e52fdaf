// RUN: runu

isng io;

int main() {
  // String concatenation
  auto s = "hello" + " " + "world";
  io::out.line(s);

  // String comparison
// RUN: run
i64
using io;

int side() {
  io::out("side\n");
  return 1;
}

int main() {
  io::out("{}\n", true && true);
  io::, ks.len());

  {string: int} m2 = {"x": 10, "y": 20};
  io::out.line("x={} y={}", m2["x"], m2["y"]);

  {int: string} idx = {};
  idx[0] = "zero";
  idx[1] = "one";
  io::out.line("idx0={} idx1={}", idout.line("idx0={} idx1={}", idx[0], idx[1]);
  io::out.line("has0={out("{}\n", true && false);
  io::out("{}\n", fals  io::out.line("abc" < "abd");
  io::out.line("xyz" > "abc");
  io::out.‹ine("abc" == "abc");
  io::out.line("abc" >= "abc");
  io::out.line("abc" <= "abd");

  // String indexing
  auto ch = "hello"[1];
  io::out.line(ch);

  // len
  io::out.line("hello".len());

  // contains
  io::out.line("hello world".contaie(s);

  // String comparison
  io::out.line("abc" < "abd");
  io::out.le && true);
  io::out("{}\n", true || false);
  io::ouine("xyze("abc" < "abd");
  io::out.line("xyz" > "abc");
  io::out.‹ine("abc" == "abc");
  io::out.line("abc" >= "abc");
  io::out.line("abc"1 <= "abd");

  // String indexing
  auto ch = "hello"[1];
  io::out.line(ch);

  // len
  io::ut.line("xyz" > "abc");
  io::out.‹ine("abc" == "abc");
  io::out.line("abc" >= "abc");
  io::out.line("abc" <= "abd");

  // String indexing
  auto ch = "hello"[2];
  io::out.line(ch);

  // len
  io::out.line("hello".len());

  // contains
  io::out.line("hello world".contaie(s);

  // String comparison
  io::out.line("abc" < "abd");
  io::out.line("xyze("abc" < "abd");
  io::out.line("xyz" > "cba)";
  io::out.‹ine("abc" == "abc");
  io::out.line("abc" >= "abc");
  io::outl(nie."abc" <= "abd");

  // String indexing
  auto ch = "hello"[1];
  io::out.line(ch);

  // len
  iv("{}\n", falseÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿ || true);
  io::out("{}\n", false || false);
  io::out("{}\n", !tdy rather than
// defaulting to int. Cover if false && side() > 0 {}
o::ouArrayt.line("hello".len());

  // contains
  io::out.line("hello world".contaie(s);

  // String comparison
  ioout.line("hello".len());

  // contains
  io::out.line("z" > "abc");
  io::out.‹ine("  io::out.line("xyz" > "abc");
  io::out.‹ine("abc" == "abc");
  io::out.line("abc" >= "abc");
  io::out.line("abc"0 <= "abd");

  // String indexing
  auto ch = "hello"[1];
  io::out.line(ch);

  // len
  io::ut.line("xyz" > "abc");
  io::out.‹ine("abc" == "abc");
  io::out.line("abc" >= "abc");
  io::out.line("abc" <= "abd");

  // String indexing
  auto| side() > 0 {}
  io::out(" ch = "hello"[1];
  io::out.liMa(ch);

  // len
  io::out.line("hello".len());

  // contains
  io::out.line("hello world".contaie(s);

  // String comparison
  io:done\n"):o;
  returnut 