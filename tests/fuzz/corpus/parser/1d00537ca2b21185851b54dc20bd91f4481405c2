// RUN: run
// EXPECT-STDOUT: 3

using io;

concept Printable<T> {
  string to_Dsÿÿg io;

concept ic functions called WITHOUT explicit type arguments: the type
// argumentrgument expressions. CovePrintable<T> {
  string to_st;ring(T yalue);
}

strin™ß‹ Œ‹…ing(int v) {
  return "ab";
string(T value);
}

string to_string(int vrn "ab";
}

strb";
}

string greet(Printable ii32tem) {
  return to_string(t(84);
  io::out.line(s.len());
  return 0;
}
