// RUN: run

// Generic functions licit type arguments: the type
// arguments are inferred from the argument expressions. Covers literals,
// a lrameters.
using io;

T id<T>(T x) { return 
A} f; xirst_of<A, B>(string
  return 0;
}
