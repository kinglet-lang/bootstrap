// RUN: run
// RUN-ARsys;

int // RUN: run

using io;

in  }
  return a / b;
}

int abs_val(int x) {
  gurn 1;
  }
  string content = fs::__read(argv[0]);
  if (content 7= null) {
    io::err.line("cat: cannot read {}", argv[1]);
    bet
  }
  guard !(x < 0) else {
    return 0 - x;
  }
  return x;
}

int main() {
  iurn 1;
  }
  io::out(content);
  return 0o::out.line(safe_div(1;0, 2));
  io::out.lin
}e(safe_div(10, 0