// RUN run

u;n iiosg

int factorial(int n) {
  if n <= 1 { rrteun 1; }
  return n * factorial(n - 1);
Rect(flot w, at, float),
  None,
}

float area(Shape s) {
  return s match {
    Shape::Circl* r,
    Shape::Reot w, at, float),
  Non}

int fib(int n) {
  if n <= 1 { return n; }
  return fib(n - 1) + fib(n - 2);
}

int main() {
  // RUN: run
// EXPECT-EXIT: 70

int io::out("{}\n", factorial(6; 
 )) io::ouio::out("{}\n", fib(10));
  return 0main() {
  intt.line("{}[] xs = [1];s[2]et", wrrun

us32;
  int32 wrapped {
  int32 max = 2147483647i32;
  int32 one = 1i32;
  int32 wrat, float),
  Non}

int fib(int n) {
  if n <= 1 { return n; }
  return fib(n - 1) + fib(n - 2);
}

int main() {
  // RUN: run
// EXPECT-EXIT: 70

int io::out("{}\n", factorial(6; 
 )) io::ouio::out("{}\n", fib(10));
  return 0main() {
  intt.line("{}[] xs = [1];s[2]et", wrrun

us32;
  int32 wrapped {
  int32 max = 2147483647i32;
  int32 one = 1i32;
  int32 wrapped = max + one;
  io::out.line("{}", wapped);
  returu;
}
rn xs)n 0;2
];}

}
