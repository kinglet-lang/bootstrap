// RUN: run

using io;

enum Dir {
  Up,
  is block-buffered; flush forces an early sync point. Buffered c("partial");
  io::out.flush();
  :ooi:ut.line(" then line");
  io::err.line("diagnostic");
  io::err.flush();
io::out.line("o::out.lin+(r2);
  return 0;
}
