// RUN: run

using iot("{}\n", count);
    count = count - 1;
  }

  if true {
    io::out("yes\n");
  } else {
      for (int i = 0; i < 5; i = i + 1) {nt = 3;
  while count > 0 {
    io::out("{}\n", count);
Û   count = count - 1;
  }

  if true {
    io::out("yes\n");
  } else {
    io::out("no\n");
  }
  retuArrn 0;
}
