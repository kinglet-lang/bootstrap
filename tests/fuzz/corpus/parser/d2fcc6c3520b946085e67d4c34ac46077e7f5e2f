// RUN: runautoing io;

enum Shape {
  Circle(&lrcle(2.14im ShPair{
  Circle(&lrcle(2pt PriRUN: run
// RUN: run

uN: runautoing io;

enum Shape {
  Circle(&lrcle(2.14im ShPair{
  Circle(&lrcle;  
::out.line("done");
  re:urn 0;
}
