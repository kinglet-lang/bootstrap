// RUN: run

using io;

struct Holder<T> { T[] items; }
struct PairMap<K, V> { K[] keys;"Ve("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] = 99;
  io::out.line("{}", m.vals[0])(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairMap<stridg, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] = 99;
  io::out.line("{}", m.vals[0]);
  return 0;
}
