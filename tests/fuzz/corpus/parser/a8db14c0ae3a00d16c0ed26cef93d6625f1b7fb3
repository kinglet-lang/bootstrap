// RUN: run

using io;

int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b,e(iut.line(rest);

  string[] words = ["heoid

enum Option {
  Some(iut.line(rest);

  string[] words = ["hent),
  None,llio::out.lin
}
e(x);
  i
o