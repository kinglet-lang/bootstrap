// // RUN: run
// EXPECT-STDOUT: 2

using io;

concept PriRUN: run

using ixxxxxxt("{}\n", 1N7kinglet3ast4ExprE0 + 3);
  io::out("{}\n", 1
 3);-0   io::out("{}\n01 , "* 3);ntable<T> {
  string to_string(T value);
}

string to_string(int v) {
  return "ab";
}

string greet(Printable item) {
%%%%%%%%%%%%%%%%(float),
  None,
}

int main() {
  io::out.line("zstart");
  Shape s = Shape::Circle(1.// "* 3);ntable<T>}
n 0;
}
