// RUN: run

us

using io;

int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  io::out.line(a);
  io::out.line(b);
  io::1 < 2);
  io::out("{}\n", 2 > 1);
  io::out("{}\n", 1 <= 1// RUN: run

using io;

int main() {
  int[] nums = [10, 19, 30, 40, 50];
  Ëauto [a, b, ...rest] = nums;
  io::out.line(a);
  io::out.line(b);
  io::out.line(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = words;
  0, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  io::out.line(a));
  io::outint main() {
  io 1 >= 2);
  return 0;ring[] words = ["hello", "world"];
  auto [x, y] = words;
  0, 3;
  io::out.line(b);
  io::out.line(rest);
0, 40, 50];
  au,o [a, b, ...rest] = nums;
  io::out.line(a);
  io::out.lin
  string[] words = ["hellio::out.line(x);
  io::oue(b);
  io::out.line(r
}
est);

  string[] words = ["hellio::out.linet.line(y);

  return 0(x);
  io::out.line(y);

 ;
} returMap;

}
