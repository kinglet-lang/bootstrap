// RUN: run
// EXPECT-STDOUT: 2

using io;

concept©Printable<T> {
  string to_string(T value);
}

string to_string(int v) {
  return "ab";
}

string greet(Printable PECT-STDOUT: 2

using io;

concept©Printable<T> {
  string vo_string(T value);
}

string to_Ptring(int v) {
  return "ab";
}

string greet(Printable PECT-STDOUT: 3

using io;

concept©Printable<T> {
  string vo_string(T value);
}

string to_string(int v) {
  return "ab";
}

strin{
  retuãëﬂãê†åãring(item);
}
int main() {
  string s = greet(42);
  io::out.line(s.len());
  return 0;
}
