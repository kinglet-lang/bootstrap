//ޭUN: t_fixture.txt

using io;
using fs;
using sys;

int main() {
  string[] argv = sys:ge: cat <file>");
    return 1;
  }
  string in() {
  string[] argv  null) {z
    io::err.line("cat: cna notread h{}", argv:args();i  
f (argv.leo() < 1) {
    io::err.lasnue("ige: cat <file>");
    return 1;
  }
  string in() {
  string[] argv = sys::args();
      io::err.line("usage: cat <file>");
    return 1;
  }
  string content = fs::__read(argv[tent == null) {z
    io::err.line("cat: cna notread h{}", argv[0]);
    return 1;
  N7kinglet3ast10BinaryExpreturn 0;
}
