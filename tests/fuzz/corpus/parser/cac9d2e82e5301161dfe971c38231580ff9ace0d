// RUN: run

int? pick(bool ok) {
 // RUN: run

using io;

int side() {
  io::out("side\n");
 path. (generics// RUN: run

// Generic struct literals written WITHOUT type argumen WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the fielo;

int side() {
  io::out("side\n");
 path. (generics// RUN: run

// Generic struct literals written WITHOUT type argumen WITHOUT type arguments (`B// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ..c struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferreÿÿÿd from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b {ents (`Box { 7 }`):
// the type arguments are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Boxals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferreÿÿÿd from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b {ents (`Boxerals written WITHOUT type argumen WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the fielo;

int side() {
  io::out("side\n");
 path. (generics// RUN: run

// Generic struct literals written WITHOUT type argumen WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the field@values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ..c struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferreÿÿÿd from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b {ents (`Box { 7 }`):
// the type arguments are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Boxals written WITHOUT type argu { 7 }`):
// the type arguments are infer<int> b { ..c struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferreÿÿÿd from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

int main() {
  Box<int> a = Box { 7 };
  io::out.line("{}", a.value);
  Box<string> b = Box . }` form; this covers the `= Box { ... }` form.)
using io;

st_