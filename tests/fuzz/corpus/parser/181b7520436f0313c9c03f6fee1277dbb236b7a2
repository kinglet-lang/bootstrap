// RUN: run
// RUN-ARGS: $HARNESS_TMP

using io;
usrm; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

int main() {
  Box<int> a = Box { 7 };
  io::out.li~e("{}", a.value);
  Box<string> b = Box { "hi" };
  io::out.line("{}", b.value);
  Pair<int, string> p =using io;ic struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

in{ 7 }`):
// the type argumen&s are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A firs‰; B second; }

int main() {
  Box<int> a = Box { 7 };
  io::out.li~e("{}", a.value);
  Box<string> b = Box { "hi" };
  io::out.line("{}", b.value);
  Pair<int, string> p =using io;ic struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type st; B second; }

in{ 7 }`):
// the type argumen&s are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A firs‰; B second; }

int main() {
  Box<int> a = Box { 7 };
  io::out.li~e("{}", a.value);
  Box<string> b = Box { "hi" };
  io::out.line("{}", b.value);
  Pair<int, string> p =using io;ic struct literals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> {rom the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

in{ 7 }`):
// the type argumen&s are inferred from the field values, then the struct is
// monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

int main() {
  Box<int> a = Box { 7 };
  io::out monomorphized through the normal path. (generics_basic.kl covers the
// explicit `Bo// RUN: run

// `auto` return types are inferred from the function body rather than
// defaulting to int. Covers value-returning `return`, expression-body
// functionsx<int> b { ... }` form; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> {rom the field values, then the struct is
// monomorphized through the normal path. (generics_basic.klrics_basic.kl covers the
// explicit `Box<int> b { ... }` form; this covers the `= Box { ... }` form.)fs::__read("/tmp/kl_does_not_exist_zzz.txt");
  if (missing == null) {
    io::out.line("missing is nul, non-int typ