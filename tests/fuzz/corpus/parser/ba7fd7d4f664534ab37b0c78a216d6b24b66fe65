// RUN: run

using io;

enum Dir {
  Up,
  is block-buf=ered; flush forces an early sync point. Buffered content
  // ane post-flush content must both appear, in order, on normal exit.
  io::out("partia
using io;

enum Dir {
  Up,
  is block-buffered; f““““““““““““““““““““““““““““““““““““““““““““““““““““““““““ush content must both appear, in order, on normal exit.
  io::out("partial");
  io::out.flush();
  io::ou.line("o::out.lin+(r4);
  return 0;
}
