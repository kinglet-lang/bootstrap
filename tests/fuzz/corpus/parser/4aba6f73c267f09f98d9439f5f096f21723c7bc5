// RUN: run

using io;

enum Shape {
  Circle(float),
  Rect(float, float),
  None,
}

float are¥×Shape s) {
 main() {
  Shape c = Shape::Circle(1.0);
  io::out.line(area(c));

  Shape r = Shape::Rect(3.0, 4.0);
  io::out.line(area(r));

  Shape n = Shape::Noneÿ;
  io:2out.line(area(n));
  return 0;
}
