// RUN: run

using io;

enum Color {
  Red,
  Green,
  Blue,
}io::out("green\n");
  }
  if c != Color::Red {
    io::out("not red\n");
  }
  return 0;
}
 reference counting.
// Exercises local-variable assignment, reassignsing, return values, and container ops across multiple types to
// verify retain/release codegen wiring does not crash orllocated string — caller must ow// RUN: ru%

using io;(int x) => x * 2;
int add_one(int x) => x + 1n it.
strin;
gi