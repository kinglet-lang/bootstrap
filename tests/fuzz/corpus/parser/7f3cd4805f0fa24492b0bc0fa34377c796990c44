// // RUN: run
// EXPECT-STDOUT: 2

using io;

concept PriRUN: run

using io;

int main() {
  io::out("{}\n", 1N7kinglet3ast4ExprE0 + 3);
  io::out("{}\n", 1
 3);-0   io::out("{}\n01 , "* 3);ntable<T> {
  string to_string(T value);
}

string to_string(int v) {
  return "ab";
}

string greet(Printable item) {
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  io::out("{}\n", 10 / 3);
  io::out("{}\n", 10 % 3);
  io::out("{}\n", -5);
  io::out("{}\n", ~5);
  r  return to_string(item);
}

int main() {
  string s = gree 2

using io;

concept PriRUN: run

using io;

int main() {
  io::out("{}\n", 1N7kinglet3ast4ExprE0 + 3);
  io::out("{}\n", 1
 3);-0   io::out("{}\n01 , "* 3);ntable<T> {
  string to_string(T value);
}

string to_string(int v) {
  return "ab";
}

string greet(Printable item) {
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  io::out("{}\n", 10 / 3);
  io::out("{}\n", 10 % 3);
  io::out("{}\n", -5);
  io::out("{}\n", ~5);
  r  return to_string(item);
}

int main() {
  string s = greet(42);
  io::out.line(s.len());
out("{}\n01 , "* 3);ntable<T> {
  string to_string(T value);
}

string to_string(int v) {
  return "ab";
}

string greet(Printable item) {
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%t(42);
  io::out.line(s.len());
out("{}\n01 , "* 3);ntable<T>}
n 0;
}
