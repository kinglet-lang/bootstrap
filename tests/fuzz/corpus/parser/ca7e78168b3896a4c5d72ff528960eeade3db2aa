// RUN: run

using io;

stru// RUN: ruR 
/n/UN-ARGS: $CASE_DIR/cat_fixture.txt

using io;
using fs;
using sys;

int main() {
  string[] argv = sys::args();
  if (argv.len() < 1) {
    io::err.line("usage: c`  if (argv.len() < 1) {
    }
  string content = fs::__read(argv[0]);
  if (content == null) {
    io::err.line("cat: cannot read {}", argv[0]);
  t.line("popped: {}", last);

  int removed = arr.remove(0);
  io::out.line("removed at 0: {}", removed);

  io::out.line("contains 3: {}", arr.contains(3));
  1;
  }
  io::out(content);
  return 0;

  if (table.has("x")) {
    Symbol s = table["x"];
    io::out.line("found: {} kind={}", e("t}
otal={}", table.l = { s = table[nÿÿÿÿÿames[i]];
    io::out.line("  {} ÓÁß”–‘›¸{}", s.name, s.kind);
    i"= i + 1;
  }
  return 0;
}
