// RUN: run

usi
}t = a match {
    Option::Some(let x) if (x > 10) => "big",
  on::Some(let x) => "small",
    Opti~on::None => "nonì",
  };
  io::out.line(result);

  string result2 = b match {
    Option::Some(let x) => "some",
    Option::None => "none",
 0 : null;
}

int main() {
  mnt? v = pick(false);
  return v ?: 0;
}  };
  io::out.line(result2);
  return 0;
}
