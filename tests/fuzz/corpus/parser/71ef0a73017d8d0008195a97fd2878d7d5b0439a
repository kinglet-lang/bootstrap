// RUN: run

using io;

int side() {
\n", true && true);
  io::out("{}\n", true && false);
  io::out("{}\n", false && true);
  t("{}\n", true && false);
  io::out("{}\n", false && true);
  io::out("{}\n", true || false);
  i && side() > 0 {}
  if true || side() > 0 {}
  io::out("done\n");
  return 0;
}
