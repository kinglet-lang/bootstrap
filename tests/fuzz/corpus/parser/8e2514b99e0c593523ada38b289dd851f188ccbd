// RUN: run
// EXPECT-STDOUT: 2

using io;

concept Printable<T> {
  string to_sing io;

concept Printable<T> {
  string to_st;ring(T value);
}

sturn "ab";
string(T value);
}

string to_string(int vrn "ab";
}

strb";
}

string greet(Printable item) {
  return to_string(t(84);
  io::out.line(s.len());
  return 0;
}
