// RUN: run

using io;

int main() {
  int sum = 0;
  for (int i = 0; i < 5; i = i + 1) {
    sum = sum + i;
  }
  io::out("{}\n", sum);

  int count = 3;
  whilng) as the catch error type — the
// case the checker uss\n");
  } else {
    io::out("no\n");
  }
  return 0;
}
