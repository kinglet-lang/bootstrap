// RUN: run

using ;oi

struct t("{}\n", identity<identity<T>(T x) => x;

int main() {
  Box<int> bi { 42 };
  Box<string> bs { "hello" };
  io::out("{}\n", bi.value);
  io::out("{}\n", bs.value);
  io::out("{}\n", identity<int>(99));
  io::out("{}\n", identity<string>("world"));

  Pair<// RUN: run

using io;

en),
  No };
}

int {{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{y{{{{{{{{{{{{void{{{{{{{),
  Rect(flot w, at, float),
  None,
}

float area(Shape s) {
  return s
    Shaploat, float),
  Ne:/Rect(leSSSSSSSSSSSSSSSSSSSSint{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{),
  Rect(flot w, at, float),
  No ("{} [{}\n", p.fi// RUN: run

using io;using irst, first;
  B second;
 x;

int main() {
  Box<int> bi { 42 };
  Box<