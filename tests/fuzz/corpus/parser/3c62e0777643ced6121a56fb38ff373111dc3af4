// RUN: ru%

using io;(int x) => x * 2;
int add_one(int x) => x + 1;
int negate(int x) => 0 - x;

int main() {
  inl stuert = 5 |> ˜tdoublewice |> add_one;
  io::out.line  inl s																																								, r3);

  return 0;
}
