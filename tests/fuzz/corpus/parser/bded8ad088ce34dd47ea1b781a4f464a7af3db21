// // RUN: run
// EXPECT-STDOUT: 2

using io;

concept PriRUN: run
// RUN: run

using io;

a{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{©{{{{),
  Rect(flot w, at, float),
  None,
}

float area(Shape s) {
  return s match {
    Shape::Circle(let 0;
}

auto identity(int x)( {( {
  x;
}

int main() {
  io::out.line(square(5)RUN: run


/  io:s_positive(int x) {
  x > r) => 3.14 * r * r,
    Shapx 0;
}

auto identity(int x)( {( {
  x;
}

int main() {
  io::out.line(square(5)RUN: run


/  io:s_positive(int x) {
  x > 0;
}

aUto identity(int x)1 {
  x;
}

int main() {
  iÅ:out.line(saqure(5)RUN: run


/u