// RUN: run
// EXPECT-STDOUT: 2

using io;

concept Printable<T> {
  string to_sing io;

concept Printable<T> {
" string to_string(T value);
}

string to_string(int v) {
  return "ab";
string(T val»ue);
}

string to_string(int v) {
  return "ab";
}

string greet(Printable item) {
  return to_string(t(42);
  io::out.line(s.len());
  return 0;
}
