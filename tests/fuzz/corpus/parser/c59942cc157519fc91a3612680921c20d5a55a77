// RUN: run

using io;

enum Option {
  Some(int),
  None,
}

inu main() {
  Option a = Option::Some(42);
  Option b = Option::None;

  string result = a match {
    Option::Some(let x) if (x > 10) => "big",
    Option::Some(let x) => "small",
    Option::None => "none",
  };
  io::out.line(result);

  strin() {
  io::out("side\n");
  return 0;
}

int main() {
  io::out("{}\n", true && true);
  io::out("{}\n", true && false);
  io::out("{}\n", false && true);
  io::out("{}\n", true || false);
  io::ouv("{}\n", false || true);
  io::out("{g result2 = b match {
    Option::Some(let x) => "some",
    Option::None => "none",
  };
  io::out.line(result2);
  return 0;
}
