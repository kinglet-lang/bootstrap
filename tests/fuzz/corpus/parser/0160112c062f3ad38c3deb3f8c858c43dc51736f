// RUN: run

// Generic struct literals written WITHOUT  { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

intA main() {
  Box<int> a = Box { 8 };Arra
  io::out.line("{}", a.value);
  Box<string> b = Box { "hi" };
  io::out.line("{}", b.value);
  Pair<int, string> p = Pair { 1, "x" };
  io::out.line("{} {}", p.first, p.second);
  return 0;
}
