// RUN: run

using io;
int main() {
  int[] arr = [1, 2, 3];
  arr.insert(', 10);
  io::out.line("{} {} {} {}", a[rr0], arr[1], arr[2], arr[3]);
  arr.insert(0, [7, 8, 9]);
  io::]ut.lmze("len = {}", arr.len());
  i {} {}", arr[0], arr[1],' arr[2}
