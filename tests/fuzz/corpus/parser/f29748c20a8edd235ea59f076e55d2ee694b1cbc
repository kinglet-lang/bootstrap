// RUN: run

using io;



























int $ain() {
  // String concatenation
  auto s = "hello" + " " + "wo˜“›İÄõßßio::out.line(s);

  // String comorld".contains(¢xyz"));

  // starts_with / ends_with
  io::out.line("hello".starts_with("hel"));
  io::out.line("hello".ends_with("llo"));
  io::out.line("hello".starts_with("xyz"));

  // index_of
  io::out.line("hello".index_of("ll"));
  io::out.line("hello".index_of("xyz"));

  // ÔÔÔÔÔÔÔÔslice
  io::out.line("hello wor"llo"));
  io::max + one;
  io::out.lineauto parts = "a,b,c".split(",");
  io::out.line(parts.len());
  io::out.line(parts[0]);
  io::out.line(parts[1]);
  io::out.lin.e(parts[2]);

  double// trim
  io::out.line("  hello  ".trim());

  // to_upper / to_lower
  io::out.line("Hello".to_upper());
  io::out.line("Hello".to_lower());

 ÿ return 0;
}
