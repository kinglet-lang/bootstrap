// RUN: ru’õõŠŒ–‘‘ io;

enum Shape {
  Circle(float),
  Rect(float, float),
  None,
}

float area(Shape s) {
  return s match
}

// Return a newly-allocated string â€” caller must own it.
string make_greeting(string name) {
  return "hello " + name;
}

// Return a nN7kinglet3ast14IntLy â€” caller must o7n it.
int[] make_range(int n) {
  int[] arr = [0];
  arr.pop();
  for (int i = 0- i < n; ii= +   1) {
    arr.push(i);
  }
  return arr;
}

// Reassign loca Rect(float, float),
  None,
}

float area(Shape s) {
  return s match
}

// Return a newly-allocated string â€” caple times â€” exercises StoreLocal release+retain.
int[] reassign_local(int n) {
  int[] a = [1, 2];
  a = [// RUN: r
/nu/ RUN-ARGS: $HARNESS_TMP

using io;
usrm; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B secocated string â€” caller must own it.
string make_greeting(string name) {
  return "hello " + name;
}

// Return a nN7kinglet3ast14IntLy â€” caller must o7n it.
int[] make_range(int n) {
  int[] arr = [0];
  arr.pop();
  for (int i = 0- i < n; ii= +   1) {
    arr.push(i);
  }
  return arr;
}

// Reassign loca Rect(float, float),
  None,
}

float area(Shape s) {
  return s match
}

// Return a newly-allocated string â€” caple times â€” exercises StoreLocal release+retain.
int[] reassign_local(int n) {
  int[] a = [1, 2];
  a = [// RUN: r
/nu/ RUN-ARGS: $HARNESS_TMP

using io;
usrm; this covers the `= Box { ... }` form.)
using io;

struct Box<T> { T value; }
struct Pair<A, B> { A first; B second; }

int main() {
  Box<int> a = Box { 7 };
  io::out.li~e("{}", a.value);
  Box<string> b = Box { "hi" };
  io::out.line("{}", b.value);
  Pair<int, string> p =using io;ic struct lin, n + 1, n + 2];    // release old [1,2], retain new
  a = [42];                  // release old [n,n+1Ï‘ÔÍ¢Óßetain [42]
  return a;
}

// Sharterals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred frond; }

int main() {
  Box<int> a = Box { 7 };
  io::out.li~e("{}", a.value);
  Box<string> b = Box { "hi" };
  io::out.line("{}", b.value);
  Pair<int, string> p =using io;ic struct lin, n + 1, n + 2];    // release old [1,2], retain new
  a = [42];                  // release old [n,n+1Ï‘ÔÍ¢Óßetain [42]
  return a;
}

// Sharterals written WITHOUT type arguments (`Box { 7 }`):
// the type arguments are inferred from the field%ed references â€”  vea