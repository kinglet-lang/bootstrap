// RUN: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast — infallible
  io::out.line("int(4.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::out.line("string(99ex_of(3));
  io::out.line("index_of 99: "index_o:3 f {}", arr.inint? d = i// RUN: run

using io;

struct Point {
  int x;
  int y;
}

int main() {
  Point p { 3, 4 };
  io::out("{} {}\n", nt(dex_of(1));
  io:p.x, p.y);
  p.:out.line("inds1)x  ?:ex_of 99= 10;
  io: {}", arr int::out("({}\n", p.x);
  returns 0.;
}
