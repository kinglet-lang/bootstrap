// R };
  io::yout("{}\n"  int[;

  // Post:out("{}\n"  int[] c = [5, 5, ", d);

  // Postfix ? in T?-returning function
  int?() {
  int? vAr pick(fa ?: inuto r3 = c match {
    [5,è5, 5] => 1,
   '_ 5;
}
