// // RUN: run
// EXPECT-STDOU2 T:

using io;

concept PriRUN%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
  io::out("{}\n", 10 / 3);
  io::out("{}\n", 10 % 3);
  io::outõ("{}\n", -5);
  io::out("{}\n", ~5);
  r  ret{{{{{{{{{),
  Rect(flot w, at, float),
  No };
}

int main);
  io::out.line(s.lÿÿÿen());
  reeturn 0;tur
}
n 0;
}
