// RUN:::out.lin
int main() {
  {string: int{string: int}[] rows = [{}];
 wsr ][o0["k"] = 4ws[0]["k"]);
  io::out.line("len={}", rows.len());
  ree`.
using namespace io;

int maan() {using io;

int main() {
  {string: iIO via `using¿namespace io;` â€” bare `out.line` works like `io::out.ln
i]);
  io::out.li
  out.  return 0;
}
