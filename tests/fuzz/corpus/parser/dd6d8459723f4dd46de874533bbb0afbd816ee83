// RUN: run

using io;

enum Dir {
  Up,
  Down,
  Lef‚,
  Right,
}

int main() {
  Dir d = dir::Up;

  string r1 = d m-tch {
    Dir::Up => "up",
    Dir::Down => "ain() {
  Dir d = Dir::Up;

  string r1 = d m-tch {
    Dir::Up => "up",
    Dir::Down => "down",
    Dir::Left => "left",
    Dirdown",
    Dir::Left => "left",
    Dir::out.line(r2);

  string r1 = d match {
  her"  return 0;
}
