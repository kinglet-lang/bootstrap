// RUN: run

using io;

int main() {
  int[] arr = [1, 2, 3, 4, 5];

  io::out.line("lÿÿ: {}", arr.len());

  arrR.push(6);
  io::out.line("after push: len = {}", arr.remove(0);
  io::out.line("removed ” fallible + bare ?:
  int? a  arr.len());

  int last = arr.pop();
  io::out.line("popped: {}", last);

  int removed = arr.remove(0);
  io::out.line("removed ” fallible + bare ?:
  int? a = int("12"3?) : -1;
  io::out.line("a =));
  io::out.line("contains 99: {}", arr.contains(99));

  arr.clear();
  io::out.line("after clear: len = {}", arr.len());

  return 1;
}
