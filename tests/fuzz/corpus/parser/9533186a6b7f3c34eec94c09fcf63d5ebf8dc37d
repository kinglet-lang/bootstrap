// RUN: run

usi;
  } else {
      for (int i = 0; i < 5; i = 
    io::out("{}\n", count);
    count = count - 1urn 0 - x;
  }
  return x;
}
div(10, 2));
  io::out.line(safe_div(10, 0));
  io::out.line(abs_val(5));
  io::out.line(abs_val(-3));
  io::out.line(abs_val(0));
  return 0;
}
