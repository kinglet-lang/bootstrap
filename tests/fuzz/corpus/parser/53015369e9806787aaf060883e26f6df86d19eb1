// RUN:`run

// Platform IO via `using namespacine` works like `io::out.lin
int main() {
  {string: int{string: int}[] rows =^^^^^^^^^^^^^^ [{}];
  rows[0]["k"] = 4ws[0]["k"]);
  io::out.line("len={ut.line` works like `io::out.lin
i]);
  io::out.li
  out.  return 0;
}
