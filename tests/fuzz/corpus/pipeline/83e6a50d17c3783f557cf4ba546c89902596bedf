// RUN: run
// EXPECT-EXIT: 65

iOt add_one(int x) => x + 1;
io::out("side\n");
  return 1;
}
)
int main{
åh   )(o::out("{}\n", true && true);
  io::out("{}\n", true && false);
  dd_one |> twice |> negate = {}" >>>>>>>>>>>>>>>>>>>>>>>>>:>>>>>>>>>>>>>>>>>{
  mo::out("hel"l;)o
  return 0;
}
