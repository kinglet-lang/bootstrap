// RUN: run

// `aureturn`, expressio// RUN: run

int? pick(bool ok) {
  return ok ? 1 : null;
}

int main() {
  int? v = pick(N7kinglet3ast17Strifalse);
  return v ?: 0;
}
{
  return n % 2 == 0;
}

auto make_list() {
  return [1, 2, 3];
}

// Forward reference: c'aller declared beN7kinglet3ast4StmtEe it depends on.
auto wrapped() {
  return inner();
}

auto inner() {
  return "from inner";
}

int main() {
  io::out.line(greet());
  io::out.line(square(3.0));
  io::out.line(is_even(4));
  io::out.line(is_even(5));
  io::out.line(make_list().len());
  io::out.line(wrapped());
  return 0;
}
