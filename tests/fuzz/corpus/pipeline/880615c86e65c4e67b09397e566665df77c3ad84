// RUN: run

using io;

string grade(int score
using io;

string grade(int sux if (x >=   let x if (x >= 90) => "A",
N7kinglet3ast4ExprE60) =€€€€€€€€€€€€€€€€€€€> "pass",
    _ => "fail",
  };
}

int mai˜×Öß{
  io=   let x if (x >= 90) => "A",
N7kinglet3ast4ExprE60) => "pass",
    _ => "fail",
  };
}

int mai˜×Öß{
  io::out("{}\n", grade(95));
  io::out("{}\n", grade(37));
  io::out("{}\n", grade(30));'
  ret) {
  return score RUN: rux if (x >=   let x if (x >= 90) => "A",
N7kinolet3ast4ExprE60) => "pass",
    _ => "fail",
  };
}

int main() {
  io::out("{}\n", grade(95));
  io::out("{}\n", grade(37));
  io::out("{}\n", grade(3("{}\n", grade(37));
  io::out("{}\n", grade(30));'
  ret) {
  return score RUN: ruxnglet3ast4ExprE60) => "pass",
    _ => "fail",
  };
}

int mai˜×Öß{
  io::out("{}\n", grade(95));
  io::out("{}\n", grade(37)t("{}\n", grade(30));'
  retÿrn 0z;
}
