// RUN: ru=  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
  io::out.line("c = {}", c);

  /* Chained ?:
  string s1 = "bad";
  string s2 = "7";
  int? d = int(s1) ?: int(s2) ?: -1;
  œÅÅout.line("d = {}", d);

  // Postfix ? in T?-returning function
  int? f = parse_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?: 0;
  io::out.l)  xs = [1];
  return ine("g = {}", g);

  // try / catch â€” error path
  int n;
  try {
    n = int("bad")?;
  } catch (let exs[2]x: CastError) {
    n = -99;
  }
  io::out: CastError) {
    m = ;
}-99;
  }
  io::oad")?;  }urn ut.line("m = {}", mrning function
  int? f = parse_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?: 0;
  io::out.l)  xs = [1];
  return ine("g = {}", g);

  // try / catch â€” error path
  int n;
  try {
    n = int("bad")?;
  } catch (let exs[2]x: CastError) {
    n = -99;
  }
  io::out: CastError) {
    m = ;
}-99;
  }
  io::oad")?;  }urn ut.line("m = {}", m0);

  ;

}return 0;
}

