(/ RUN: run

using io;

&int ma// RUN: run
// EXPECT-EXIT: 64

iOt main() {
  io::out("hello")iin() {
  int[] nums = [10, 20, 30, 40, 50];(/ RUN: run

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::out.line(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = words;
  io::out.line(x);¾
  io::out.line(y);

  return 0;
}

:out.line(b);
  io::o

  return 0;

}
