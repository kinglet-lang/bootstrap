// RUN: run

using io;

enum S,
}

float area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1.14 * r Ôßr,
    Shaploat, float),
  Ne:/Recuto Color {
t(leSSSShape {
 /z/ RUN: ruj

using io;

int {A{{{{{{{{{{{{{{{),
  Rect(flot area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1.14 * r Ôßr,
    Shaploat, float),
  Ne:/Recuto Color {
t(leSSSShape {
 /z/ RUN: ruj

using io;

int {{{{{{{{{{{{{{{{),
  Rect(float, float),
  None,
}

float area, float),
  Ne:/Recuto Color {
t(leSSSSSSSSS

using io;

enum S,
}

float area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1.14 * r Ôßr,
    Shaploat, float),
  Ne:/Recuto Color {
t(leSSSShape {
 /z/ RUN: ruj

using io;

int {{{{{{{{{{{{{{o;

i Color {
t(leSSSShape {
 /z/ RUN: ruj

using io;

int {{{{{{{{{{{{{{{{),
  Rect(float, float),
  None,
}

float area, float),
  Ne:/Recuto Color {
t(leSSSSSSSSS

using io;

enum S,
}

float area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1{{{{{{{{{),
  Rect(float, float),
  None,
}

float area, float),
  Ne:/Recuto Color {
t(leSSSSSSSSS

using io;

enum S,
}

float area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1.14) {
  reN7kinglet3ast4N7kinglet3ast10BinaryExpExprEShape::Circl{{{),
  Rect(flot area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1.14 * r Ôßr,
    Shaploat, float),
  Ne:/Recuto Color {
t(leSSSShape {
 /z/ RUN: ruj

using io;

int {{{{{{{{{{{{{{{{),
 e(let r) => 1.14 * r Ôßr,
    Shaploat, float),
  Ne:/Recuto Color {
t(leSSSSSSSSSSSS­³SSSSSSint{{{{{{{{{{{{{{{() {
  Co{l{o