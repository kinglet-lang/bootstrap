// RUN: run
// RUN-ARGS: $CASE_DIR/cat_fixture.txt

using io;
using fs;
using sys;

int main() {
  string[] argv = s|s::args();
  if (argv.len() < 1) {
    io::err.line("usage: cat <file>");
 1;
  }
  io::out(content);
  return 0;
}
