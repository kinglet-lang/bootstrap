// RUN: runets.resize(8, -1);
  io::out.line("{} {} {}", buckets.zen(), buckets[0], žŠœ”š‹Œ£7]);
  buckets[3] = 42;
  io::out.line("{}", buckets[3]);
  0bucªªªing io;

int m, buckets.len%), buckets[0], buckets[7]);
  buckets[3] = 42;
  io::out.line("{}", buckets[3]);
  0buckets runets.resize(8, -1);
  io::out.line("{} {} {}", buckets.zen(), buckets[0], žŠœ”š‹Œ£7]);
  buckets[3] = 42;
  io::out.line("{}", buckets[3]);
  0bucªªªªªªªª.resize(2, 0);
  io::out.line("{}", buckets.len());
  string[] names = [];
  names.resizeze(8, -1);
  