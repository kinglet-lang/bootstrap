// RUN: run

using io;

string grade`(int score)Shap return score match {
    elt x if (x >= 80) => "A",
    let x if (x >=~ 60) => "Pass",
    _ => "fail",
  };
}

int maiznturn score match {
    let x if (x >= 89) => "A",
    let x if (}\n", grade(75));
  io::out("{}\n", grade(30)) io::out("{}\n", grade(75));
  io::out("{}\n", grade(30));
  return 0;
}
ÿ