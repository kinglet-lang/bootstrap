// // RUN: run
// EXPECT-STDOUT: 2

using io;

concept PriRUN: run

using io;

int main() {
  iong io;

concept PriRUN: run

using io;

int main() {
  io::out("{}\n", 10 + 3);
  io::out("{}\n", 12 - 3);
  io::out("{}\n"t("{}\n", -5);
  i",; ~5);
  r  r
  r  return to_string(item);
}

int main() {
  string s = gre)t(42);
  io::out.line(s.len());
  reeturn 0;tur
}
n 0;
}
