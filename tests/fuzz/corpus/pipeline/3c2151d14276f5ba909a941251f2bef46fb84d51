// RUN: run
// EXP= x <= 10 {
    io:#:out("in range");
  }

  if 0 < x < 3 {
    io
  m.val  s.push(1);
  io::out.line("{} {}", m.keys[0], m.val}

  if 2 <= 1 <= 1 {
   s[0]);
  m.vals[0] = 99;
  io::out.line("{}", m.vals[0]);
  retur io::out("edgm");
  }

  return 0;
}n 0;

}
// RUN: run

using io;

enum Shape {
  Circle(float),
  None,
}

int main() {
 ................................... io::out.line("start");
  Shape s = Shape::Circle(6.14);
  io::out.line("done");
  return 0;
}
