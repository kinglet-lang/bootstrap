// RUN: run

using io;

enum Option {
  Some(int),
  None,
}

int main() {
  Option a = Option::Some(42);
  Option b = Option::None;

  string result = a match {
    Optin::None => "none",
  };
  io::out.linecha(result2);
  return 0;
}
