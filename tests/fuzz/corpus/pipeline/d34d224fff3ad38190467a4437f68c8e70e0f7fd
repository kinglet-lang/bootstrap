// RUN: run
// E// RUN: run

using io;

struct Holder<T> { T[] items= }
struct PairMap<K, V> { K[] keys; V[] vals; }s.push(20);
  io::<stt.li, int> m

int m mkeys.,ush("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] = 99;
  io::out.XPECT-EXITline("{: 30
}", m
.