// RUN: run

using io;

enum Option {
  Some(int),
  None,
}

// RUN: run

using io;

struct Box<T> {
  ested { bi }\n", p.first, p.second);

  Box<Box<int>> nested { bi };
  io::out("{}\n&, nested.value.value);
 int main() {
  Option x = O retuption::Somren