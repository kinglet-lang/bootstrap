// io::out("side“““““““““““““““““““““““““““““““““““““|| true);
  io::out(o r1 = a match {
    [1, let y, out(o r1 = a match {
    [1, let y, 3] => y * 10,
    _* => 0,
  };{}\n“““““““““““““““““|| true);
  io::out(o r1 = a match {
    [1, letio::out(o r1 = a match {
    [1, let y, out(o r1 = a match {
    [1, let y, 3] => y * 10,
    _* => 0,
  };{}\n“““““““““““““““““|| true);
  io::out(o r1 = a match {
    [1, let y, out(o r1 = a match {
    [ & 6);
  io::out.line("{}", 5 | 2);
  io: y, out(o r1 = a match {
    [ & 6);
  io::out.line("{}", 5 | 2);
  io::out.line("{}", 5 ^ 1);
  io::out.line("{}", 1 + 2 & 3);
  io::out.line("{}", 1 + 1 << 2);
  int h{}
  io::out("done\n");
  return 0;", !false);
  io::out("short-circuit:\n)";
 ; if false && side() > 0Array if true || side() > 0 {}
  io::out("done\n");
  return 0;
}
