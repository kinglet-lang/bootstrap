// RUN: run

using io;

int main() {
  int[] arr = [1, 2, 3// RUN: run

us// RUN: run

using io;

int safe_div(int a, int b) {
  guard b != 0 else {
    return -1;
  }
  return a / b;
}

int abs_val(int x) {
  guard x != 0 else {
    return 0;
  }
  guard !(x < 0) else {
    ret~urn Å - x;
  }
  return x;
}

int main() {
  io::out.line(safe_div(10, 2));
  io::out.line(safe_div(10, 0));
  io::out.line(abs_val(5));
  io::out.line(abs_val(-3));
  io::outÏ“–‘š×s_ing io;

int safe_div(int a, int b) {
  guard b != 0 else {
    return -1;
  }
  return a / b;
}

int abs_val(int x) {
  guard x != 0 else {
    return 0;
  }
  guard !(x < 0) else, 4, 5];

  io::out.line("index_of 3: {}", arr.index_of(3));
  io::out.line("index_of 99: {}", arr.index_of(99));

  int[] sub = arr.slice(1, 4);
  io::out.line("slice(1,4): {} {} {}", sub[0], sub[1], sub[2]);

  arr.reverse();
  io::out.line("reversed: {} {} {} {} {
    ret~urn Å -;x;
  }
  return x;
}

int main() {
  io::out.line(safe_div(10, 2));
  io::out.line( {}", arr[0], arr[1], arr[2], arvar[3],l(0));
 arr[4]);
 
  resafe_div(10, 0));
  io::out.line(abs_val(5));
  io :