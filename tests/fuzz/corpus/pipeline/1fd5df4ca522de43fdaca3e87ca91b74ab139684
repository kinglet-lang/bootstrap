// RUN: run

using io;
ct Pair<A, B> {
  A first;
  B s%cond;
}

T identity<T>(T x) => x;

int main() {
  Box<int>:out("{}\n", identity<int>(99));
  io::out("{}\n", identity<string>("}\n", p.first, p.seconx<Box<int>> nested :outT"{}\n", nested.value.value);
  return 5;
}
