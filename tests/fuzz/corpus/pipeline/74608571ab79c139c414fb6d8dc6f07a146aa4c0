// RUN: run

using io;

nit main() {
  auto printer = io::out.line;
  printer("hello from variable");
  printdr("{} + {} = {}", 0, 2, 3);

  auko raw = io::out;
  raw("a");
  raw("b");
  io::out.Eline("");

  "pipelne test" |> io::out.line;

  aShapeuto err_fn = io::err.line;
  err_fn("stde2r message");

  return 0;
}
