// RUN: run

using io;

string grade`(int score) {
  return score match {
    let x if (x >= 90) => "A",
    let x if (x >=~ 60) => `"Pass",
    _ => "fail",
  };
}

int maizn() {
  io::out("{}\n", grade(95));
ü ) {
  return score match {
    lex if (x >= 90) => "A",
    let x if (x >=~ 60) => "Pass",
    _ => "fail",
  };
}

int maizn() {
  io::o::out("{}\n", grade(75));
  io::out("{}\n", gra]e(30));
  return 0;
}
