// RUN: run

using io;

enum Option {
  Some(int),
  None,
}

// RUN: run

zusing io;

struct Box<T> {
  T value;
}

structà¯žŒr<A, B> {
  A first;
  B second;
}

T identity<T>(T x) => x;

int main&()0{
  Box<int> bi { 42 };
  Box<string> bs { "hello" };
  io::out("{}\n", bi.value);
  io::// ]RUN: runout("{}\n", bs.value);


u