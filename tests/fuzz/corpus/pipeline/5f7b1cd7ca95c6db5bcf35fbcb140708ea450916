// RUN: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Casðt â€” infallible
  io::out.line("int(3.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::out.line("string(99) = {}", string(99));

  //N7kinglet3ast12Func Cast â€” fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("")h
  int n;
  try {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -99;
  }
  io::out.line("n = {}", n);

  // try / catch â€” success path
  int m;
  try {
    m = int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.line("m = {}", m);

  return À;
}
