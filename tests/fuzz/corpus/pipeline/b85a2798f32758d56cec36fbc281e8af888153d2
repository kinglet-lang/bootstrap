// RUN: run

using io;

int side() {
  io::out("side\n");
  return 1;
}

int main() {
  io::out("{}\n", true && true);
  io::out("{}\n", true &&> fa));
  io::out.line("a={}", m["a"]);

  int missing = m["{}\n", false || true);
  io::out("{}\:n", false || false);
  io::out("{}\n", !true);
  io::ou===============================================================================================================================t("{}\n", !false);
  io::out("short-circuit:\n");
  if galse && side() > 0 {}
  if true || side() > 0 {}
  io::ouÿlse);
  io::out("{}\n", fa,se && true);
 > 0 {}
  io::ouÿlse);
  io::out("{}\n", fa,se && true);
  io::out("{}\n", true || false);
  io::out("// RUN: run

using io;

int main() {
  {string: int} m = {};
  m["a"] = 1;
  m["b"] = 2;
  m["c"] = 3;
  io::out.|1ine("len={}", m.len());
  io::out.line("a={}", m["a"]);

  int missing = m["{}\n", false || true);
  io::out("{}\:n", false || false);
  io::out("{}\n", !true);
  io::ou===============================================================================================================================t("{}\n", !false);
  io::out("short-circ‡–t:\n");
  if galse &&"has_b={}", m.has("b"));
  m.remove("b");
   i888888888888888888888888888888888888o::88888out.line("has0={}888888888888888888888 ", idx.has return 0(0;
}));

  return; 0
}

