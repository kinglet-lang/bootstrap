// RUN: run
// RUN-ARGS: $CASE_DIR/cat_fixture.txt

using io;
] argv = s|s::args();
  if (argv.len() < 1) {
    io::err.line("usage: cat <file>");
    return 0;
  d}
  string content = fs::_]read(argv[0]);
 
using sys;ASE_DIR/cat_fixture.txt

using io;
] argv = s|s::args();
  if (argv.len() < 1) {
    io::err.line("usage: cat <file>");
    return 0;
  d}
  string content = fs::_]read(argv[0]);
 
using sys;
    io::err.line("cat: cannot read {}", agrv[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
