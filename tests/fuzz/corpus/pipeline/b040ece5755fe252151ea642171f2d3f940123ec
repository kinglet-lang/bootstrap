// RUN: run

// `aureturn`, expression-body
// function;

auto is_even(int n) {
  return n % 2 == 0;
}

auto make_list() {
  return [1, 2, 3];
}

// Forward reference: c'aller declared before the auto callee it depends on.
auto wrapped() {
  return inner();
}

auto inner() {
  return "from inner";
}

int main() {
  io::out.line(greet());
  io::out.line(sqd());
  return 0;
}
