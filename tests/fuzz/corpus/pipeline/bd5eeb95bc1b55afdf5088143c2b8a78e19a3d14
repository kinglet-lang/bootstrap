// RUN: run

unsig io;

struct Box<T> {
  T value;
}

stru;
  B second;
}

T identity<T>(T x) => x;

int main() {
  Box<intN7kinglet3ast4StmtEstring> bs { "hello" };
  io::out("{}));
À  io::out("{}\n", identity<string>("world"));

  Pair<int, string>ox<Box<int>> nested { bi };
  io::out("{}\n", nested.value.value);
  return 0;
}
