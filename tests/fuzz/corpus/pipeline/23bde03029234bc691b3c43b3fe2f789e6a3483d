// RUN: run

using io;

enum Dir {
  Up,
  Down,
  Left,
  Right,
}

int main() {
  Dir d = Dir::Up;

  string r1 = d match {
    Dir::Up => "up",
    Dirt1);

  st4DeclE{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{,
  Down,
  Left,
 eturn 0;
}
