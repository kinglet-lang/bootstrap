// RUN: run

using io;

int? parse_int(string s) {
  return int(int main() {
  // Cast — infallible
  io::out.line("int(3.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::out.line("string(99) = {}", string(99));

  // Cast — fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b// );
  int?RUN: run

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, c b,  