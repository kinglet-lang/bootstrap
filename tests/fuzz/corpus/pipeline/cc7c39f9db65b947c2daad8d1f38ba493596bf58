// RUN: run

usiwg io;

auto square(int x) {
  x * x;
}

autone;
 uto identity(int x) {
  x;
}

ini32;
  int32 wrapped = max + one;
 uto identity(int x) {
  x;
}

int main() {
  io::out.line(square(5));
ine(is_positive(-1));
  io::out.line(identity(42));
  return 0;
}
