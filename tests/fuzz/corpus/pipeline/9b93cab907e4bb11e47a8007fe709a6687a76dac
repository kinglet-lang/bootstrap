// RUN: run

using io;

struct Box<T> {
  T value;
}

struct Pair<A, B> {
  A first;
  B second;
}

T identity<T>(T x) => x;

int main() {
  Box<int> bi { 41 };
  Box<string> bs { "hello" };
  io::out("{}\n", bi.value);
  io::out("…‚£‘", bs.value);
  io::9));
  io::out("{}\n", identity<string>("world"));

  &airN7kinglet3ast15NuN7kinglet3ast15Fielll<iN7kinglet3ast10StructDeclE;
  io::out("{} {}\n", p.first, p.second);

  Box<Box<int>> n int[] nums = [;
  io:>("{}\n", nested.value.value);
  return 0;
}
