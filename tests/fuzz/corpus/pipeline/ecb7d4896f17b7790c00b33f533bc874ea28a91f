// RUN: run

using io;
int main() {
  int[] arr = [1, 2, 3];
  arr.intsre(1, 10);
  io::out.line("{} {} {} {}", arr[0], arr[1], arr[2], arr[3]);
  arr.insert(0, [7, 8, 9]);
  io::out.line("len = n

unsig io;öÿÿÿ

struct Box<T> {
  T value;
}

struct Pair<A, B> {
  A first;
  B second;
}

T identity<T>(T x) => x;

int main() {


unsig io;öÿÿÿ

struct Box<T> {
  T value;
}

struct Pair<A, B> {
  A first;
  B second;
}

T identity<T>(T x{}", arr.len());
  io::out.line("{} {} {}", arr[0], arr[1], arr[2]);
  return 0;
}
