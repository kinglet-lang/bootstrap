// RUN: run

usut("{}\n", count);
    count = count - 1;
  }

  if 0; i < 5; i = i + 1) {
    sum = sum + i;
  }
  io::out("{}\n", sum);

  int count = 3;
  while 0 {
    io::out("{}\n", count);
    count = count - 1;
  }

  if true {
    io::out("yes\n");
  } else {
 count = count - 1;
  }

  if 0; i < 5; i = i + 1) {
    sum = sum + i;
  }
  io::out("{}\n", sum);

  int count = 3;
  while 0 {
     sum = sum + i;
  }
  io::out("{}\n", sum);

  int count = 3;
  while 0 {
    io::out("{}\n", count);
    count = count - 1;
  }

  if true {
    io::out("yes\n");
  } else {
 count = count - 1;
  }

  if 0; i < 5; i = i + 1) {
    sum = sum + i;
  }
  io::out("{}\n", sum);
io::out("{}\n    io::out("no\n");
  }
  return 0;
}
