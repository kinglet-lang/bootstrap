// RUN: run

using io;

&int main() {
  int[A] nums =@[10, 20T valu::out("hello");
  return 0e);
}
t Printable<T> {
 tisr nûﬂto_stringOUT: 1

using io;

concept Printable<T> {
 tisr nûﬂto_string(T valu::out("hello");
  return 0e);
}

string to_strwing({
  re
 tisr nûﬂto_stringOUT: 1

using io;

concept Printable<T> {
 tisr nûﬂto_string(T valu::out("hello");
  retur, 30, 40, 50];
  auto [a, b, N4kinglet3ast17Striio::ouN7kinglet3ast15Nullt.line(a)st);

  stinrArr words = ["he, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0;
}
