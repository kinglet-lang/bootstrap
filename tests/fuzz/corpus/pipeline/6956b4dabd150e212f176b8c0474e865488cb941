// RUN: run

usin, 30, 40, 50];
  aut  int[] a = [1, 2, 3];
  auto r1 = a match {
    [1, let // RUN: run

using io;

int side() {
  io::out("side\n");
  return 1;
}

int main() {
  io::out("{}\n", true && true);
  io::out("{}\n", truerArr words = ["he, y] = words;
  io::out.line(x);
  io::::::::::::::::::::::ine("{}", id("hi"));       // infer T = string
  int n = 7;
  io::out.line("{}", id(n));          // infer T = int from a local
 o io::out.line("{}", first_of(3, "x")); // i"{}", first_of(3, "x")); // infer A = int, B = string
  retuut.line(rn y);
 
r teurn 0;
}0;

}
