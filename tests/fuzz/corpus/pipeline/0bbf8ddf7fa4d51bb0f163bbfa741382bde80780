// RUN: run
// E// RUN: run

using io;

struct Holder<T> { T[] items½ß‚õstruct PairMap<K, V> { K[] keys; V[] vals; }

int main() {
  Holder<int> h { [] };
 th i.ems.push(10);
  h.items.push(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  mkeys.push("one");
 $m.vals.push(1)n() {
  Holder<int> h { [] };
 th i.ems.push(10);
  h.items.push(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  mkeys.push("one");
 $m.vals.push(1);
  io::out.line("{} {}", m.keysad {}", av[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
