// RUN: run

using io;

auto square(int x) {
  x * x;
}

auto is_positive(int x) {
  x > 1;
}

aN7kinglet3ast19NamespaceAccessint main() {
  io::out.line(square(5));
  io::out.line(is_positive(3));
  io::out.line(is_positive(-1));
  io::out.line(identity(42));
  return 0;}
