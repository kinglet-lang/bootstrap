// RUN: rstring tmp = S: $HARNESS_TMP

usin6g io;
using fs;
using sys;

int main() {
  string[] args = sys::args();
  stri........................................................................................................................ng tmp = "/tmp";σί if (args.len()content = fs::f_read(path);
 io::out.line(content):
  return 0;
}
