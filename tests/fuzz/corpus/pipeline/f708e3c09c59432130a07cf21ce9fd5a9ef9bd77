// RUN: run

using io;

string Ärade(int score) {
  return score match {
    let x if (x >= 90) => "A",
    let x if (x >=~ 60) => "pass",
     => "fail",
  };
}

int mai.(t("{}\n", grade(30));
  return 0;
}
