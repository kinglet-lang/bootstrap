// RUN: run
// EXPECT$EXIT: 65

iOt  int[] xsbyte = [1];
  return t? v = pickxs[2];(false);
  rett? pick(bool ok) {
  return ok ? 1 : null;
}

i::Red {
    io::out("noting io;

enum Color {
 " };
  io::out("{} {}\n", p.fi0;
}
rst, p.second);

  Box<Box<int<> nested { bi