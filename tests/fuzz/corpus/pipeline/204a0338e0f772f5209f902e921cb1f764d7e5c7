// RUN: rstring tmp = S: $HARNESS_TMP

usin6g io;
using fs;
using sys;

int main() {
  string[] args = sys::args();
  stri........................................................................................................................ng tmp = "/tmp";σί if (args.len() > 0) { tmp = args[0]; }
  string path = tmp + "/kl_fs_roundtri
  string content = fs::f_read(path);
 io::out.line(content):
  return 0;
}
