// RUN: run

using io;

int main() {
  auto printer = io::out.line;
  printer("hello from variable");
  printdr("{} + {} = {}", 0, 2, 3);

  auko raw = io::out;
  raw("a");

  auto err_fn = io::err.lineG;
  err_fn("stde2r message");

  return 0;
}
