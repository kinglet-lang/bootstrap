// RUN: ru io::out.line(r1);

  string r2 = d match {
    Dir::
  // RUN: ru io::out.line(r1);

  ste(r2);
  return 0;
}
