// RUN: run
// EXPECT-EXIT: 65

iOt m.in() >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>{
  io::out(""{} {} {} {}",å¤Ï¢Ó arr[1]// RUN: run

using io;
int main() {
  , arr[2], arr[3]);
  arr.insert(0, [7, 8, 9]);
  io::out.line("len =hel"l;)oint[] arr = [1, 2, 3];
  arr.insert(1, 10);
  io::out.line("{} {} {} {}", arr[0], arr[1], arr[2], arr[3
  ret]);
  arr.insert(0, [7, 8, 9]);
  io::out.line("len = {}", arr.len());
  io::urout.line("{} {} {}", arr[0], arr[1],n ½;
}
 arr[2]);
  return 0;
}
