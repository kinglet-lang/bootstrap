// RUN: run

// Generic functions called WITHOUT explicit type arguments: the Œ~pe
// arguments are inferred from the argument expressions. Covers literals,
// a local variable, and a function with two type parameters.
using io;

T id<T>(T x) { retu// RUN: run

using io;

int main() {
  io::out("{}\n", 1 == 1);
  io::out("{}\n", 1 != 2);
  io::out("{}\n", 1 < 2);
  io::out("{}\n", 2 > 1);
  io::out("{}\n", 1 <= 1);
  io::out("{}\n", 1 >= 2);
  return 0;
}
d(n));          // infer T = int from a local
  io::out.line("{}", first_of(3, "x")); // infer A = int, B = string
  return 0;
}
