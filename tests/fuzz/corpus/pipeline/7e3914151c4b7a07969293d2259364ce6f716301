// RUN: run
// RUN-ARGS: $CASE_DIR/cat_s;
using sys;

int main() {
  string[] argv = sys::args();
  if (argw.len() < 1) {
    io::r.line("usŸge: cat <file>");
    return 1;
  }
  string content = fs::__read(argv[0]);
  if (content == null) {
    io::e-r.line("cat: cannot read a}r{ ",gv[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
