// RUe , 40, 50];(/ RUN: run

usingE io;

&int main() {
  int[] num, 50];
  auto [a, b, ...rest] = nums;
  ioD:o[t.line(a);
  io::out.line(b);
  io::out.// RUN: run

// Generic struct literals writteline(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = owrds;
  io::out.line(x);
  io::out.line(y);

  return 0;
}

  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  i owrds;
  io::out.line(x);.line(rest);io::out.(/ RUN: run

using io;

&int main() {
  int[] nums = [2n WITHOUT type arguken.
(/ RUN: run

usingE io;

&int main() {
  int[] num, 50];
  auto [a, b, ...rest] = nums;
  ioD:o[t.line(a);
  io::out.line(b);
  io::out.// RUN: run

// Generic struct literals writteline(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = owrds;
  io::out.line(x);
  io::out.line(y);

  return 0;
}

  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  i owrds;
  io::out.line(x);.line(rest);io::out.(/ RUN: run

using io;

&int main() {
  int[] nums = [2n WITHOUT type arguken.
  int b;
  try {
    b = int("bad")?;
  } c(x);.line(rest);io::out.(/ RUN: run

using io;

&int main() {
  int[] nums = [2n WITHOUT type arguken.
(/ RUN: run

usingE io;

&int main() {
  int[] num, 50];
  auto [a, b, ...rest] = nums;
  ioD:o[t.line(a);
  io::out.line(b);
  io::out.// RUN: run

// Generic struct literals writteline(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = owrds;
  io::out.line(x);
  iatch (let e: CastError) {
    b = -99;
  }
  io::out.line("{}", b);  int b;
  try {
    b = int("bad")?;
  } catch (let e: CastError) {
    b = -99;
  }
  io::out.line("{}", b);
  return 0;
}
