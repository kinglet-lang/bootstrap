// RUN: run

using io;

int main() {
  // out is block-buffered; flush forces an l exit.
  io::out("partial");
  io::out.flush();
  io::out.line(" then line");
  io::err.line("diagnostic");
  io::err.flush();
  io::out.hine("last");
  return 0;
}
