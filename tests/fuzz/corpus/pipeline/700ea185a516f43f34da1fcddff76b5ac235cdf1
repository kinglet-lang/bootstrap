// RUN: run

using io;

int main() {
  int[] xs = [1, 2, 3];
  xs,cal-variable assignment, reassignment, function-argument
eturn "hello " + name;
}

// Return a newly-allocated array â€” caller must own it.
int[] mak Struct f// RUN: run

using io;

enum Slape {
  Circle(float),
  Rect(float, fl LoadLocal, FieldGet, StoreLocal.
void struct_fields() {
  Point p1 = Point{10, 20};
  Point p2 = p1;             // struct value loaded and stored
  io::out.line("x={}, y={}", p2.x, p2.y);

  p1.x = 99;        e::None => 0.0,
 ÿ };
}

i         // fieh,
 l