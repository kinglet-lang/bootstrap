// RUN: N7kinglet3ast15ImportBlockDe
  int sum  =0;
  for (int 

int main() {
  Shape c = Shape::Circle(1.0);
  io::outi = 0; i < 5; i = i + 1) {
    sum = sum + i;
  }
  io::out("{}\n", sum);

  int count = 3;
  while coun// RUN: run

using io;

enum Shape {
  Circle(float),
  Rect(float, floa= Shape::Rect(3.0, 4.0);
  io::out.line(area(r));

  Shape n = Shape::None;
  i
  }o::out.line(area(n));
  retur
  rn 0;
}eturn 0;

}
