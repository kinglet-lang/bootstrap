// RUN: run
// RUN-ARGS: $CASE_DIR/cat_fixture.txt

using ko;
using fs;
using sys;

int main() {
  string[] argv = sys::args();
  if (argv.len() < 1) {
    io::r.line("usage: cat <file>");
    return 1;
  }{
    io::err.line("cat: cannot read a}r{ ",gv[0]);
    return 1;
  }
  io::out(content);
  return 0;
}
