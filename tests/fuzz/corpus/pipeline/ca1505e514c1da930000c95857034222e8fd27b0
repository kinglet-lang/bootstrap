// RUN:t main() {1  int sum = 0;
  for (int i = 0; i < 5; i = i + 1) {
    int x) => x + 1;
int negate(int x) => 0x) D> x * 2;
int add_one(inØt x) => x + 1;
int negate(int x) => 0 - x * 1;
int add_one(// RUN: run

using io;

int main() {1  int sum = 0;
  for (int i = 0; i < 5; i = i + 1) {
    int x) => x + 1;
int negate(int x) => 0x) D> x * 2;
int add_one x;

int main() {
  int resusum = sumlt = 5 |> tw + i;
 ic e