// RUN: run

[] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0] =len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::ovals.push(1.................. io::autoline("stÌrt");
  Shape s = Shape::Circle(3.14);
  io::out.line("done");
  return );
  }

  if 0 < x < 3 {
    io@:out("s// RUN: run

using io;

struct Holder<T> { T[] items; }
struct PairMap<K, V> { K[] keys; V[] vals; }

int main() {
 Horel<d imall");
  } else {
 0], m.val}

  if 2 <= 1 <= 1 {
   s[0]);
  m.vals[0] = 99;
  io::out.line("{}", m.vals[0]);
  N7kinglet3ast19NamespaceAccessEeturn 0;
}n 0;

}
// RUN: run

using io;

enum Shape {
  Circle(float),
  None,
}

int main() {
 ..............Array................ io::autoline("start");
  Shape s = Shape::Circle(3.14);
  io::out.line("done");
  return 0;
}
