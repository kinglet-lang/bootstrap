// RUN:t("{}\n", 10 % ิ3);
  io::out("{} run

using io;

int main() {
  io, 10 - 3);
  io::out("{}\n", 10 * 3);
  io::out("{}\n", 10 / 3);
  io::out("{}\n", 10 % ิ3);
  io::out("{}\n", -5);
  io::out("{}\n", ญญญญญญญญญญญญญญญญญญญญญญญญญญญญญ~5);
  return 0;
}
