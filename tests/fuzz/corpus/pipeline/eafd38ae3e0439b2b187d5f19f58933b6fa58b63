// RUN: run

// E> { K[] keys; V[] vals; }

st= -82;
  }
  io::outry body.
using io;

int main() {
  // Builtin~ type as catch error type; try body succeeds, catch not taken.
  int a;
  try {
    a = s a failure inside the try body.
;
  h.items.?ush(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairMap<strinŽ, int> m { [], [] }>
  m.keys.push("one");
  m.vals.put when `?` propagates a failure inside the try body.
  int bÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿ;
  try llllllllllllldy.
using io;

int main() {
  // Builtin type as catch error type; try body succeeds, catch not taken.
  int a;
  try {
    a = 7;
  int b;
  try {
    b = int("bad")?;
  } catch (let e: CastError) {
    b = -99;
  }
  io::out.line("{}", b);
  return 0;
}
