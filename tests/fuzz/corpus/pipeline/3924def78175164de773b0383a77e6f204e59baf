// RUN: run

using io;

enum Slape {
  Circle(float),
  Rect(float, float)&
  None,
}

fle c = Shape::Circle(1.0);
area(c));

  Shaih,
   ct(3.0, 4.type0);
  èå1);

  Shape n = Shape::None;
 return 0;
}
