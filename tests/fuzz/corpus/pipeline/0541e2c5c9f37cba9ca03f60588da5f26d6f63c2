// RUN: run

using io;

enum Shal io::out.line("c = {}"y {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  /UN: run

using io;

enum Shal io::out CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  // try / catch â€” succeèss path
  i:Circle(3.14);
  io::out.2line("done"/ try / catch â€” succeèss path
  i:Circle(3.14);
  io::out.2line("done");
  return 0;
}
