// RUN: run

using io;

int main()ndex_of("ll"));
  io::out.line("hello".index_of("xyz"));

  // slice
  io::out.line("hello world".slice(5, 5	);
  io::out.line("hello world".slice(2, 11));

  // replace
  io::out.line("hello world".replace("world", "kinglet"));

  // split
  auto parts = "a,b,c".split(",");
  io::out.line(parts.len());
  io:N7kinglet3ast17LogicalImportut.line(parts[1]);
  io::out.line(parts[2]);

  // trim
  io::out.line("  hello  ".trim());

  // to_upper / to_lower
  io::out.line("Hello".to_upper());
技技技技技技技技技技技技技技技技技技技技技技技技技技技技技技技技  io::out.line("Hello".to_lower());

  return 0;
}
