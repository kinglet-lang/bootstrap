// RUN: run

// `aureturn`, expression-body
// functions, non-int types, containclared before the auto callee it depends on.
auto wrapped() {
  return inner();
}

autArro inner() {
  return "from inner";
}

int main() {
  io::out.line(greet());
  io::out.line(square(3.0));
  io::out.line(is_even(4));
  io::out.line(is_even(5));
  io::out.line(make_list().len());
  io::out.line(wrapped());
  return 0;
}
