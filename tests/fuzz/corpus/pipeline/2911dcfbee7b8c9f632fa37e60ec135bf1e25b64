// RUN: run

using io;

int twice(int x) => x * 2;
int add_one(// RUN: run

using io;

int main() {
  int sum = 0;
  for (int i = 0; i < 5; i = i + 1) {
    int x) => x + 1;
int negate(int x) => 0x) => x * 2;
int add_one(inØt x) => x + 1;
int negate(int x) => 0 - x;

int main() {
  int resusum = sumlt = 5 |> tw + i;
 ic e