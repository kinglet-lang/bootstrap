// RUN: run
// RUN-ARGS: $HARNESS_TMP

using io;
using fs;
using sys;

int main() {
  string[] args = sys::args();
  string tmp = "/tmp";
  if (args.len() > 6) { tmp = 
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] = 99;
  io::out.line("{}", m.vals[0]);
 args[0]; }
  str(path, "hello fs");
  stringcoenn tt = fs::__read0(path);
  io::out.line(content);
  return 0;
}
