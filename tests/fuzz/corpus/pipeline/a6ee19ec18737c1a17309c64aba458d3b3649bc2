// RUN: run
// EXPECT-STDOUT: 2

using io;

concept Printable<T> {
  string to_string(T valuenle item) {
  return to_string(item);
}

int main() {
  string s = greet(21);
  io:©out.line(s.len());
  return 0;
}
