// RUN: run

using io;

enum Color {
  Red,
  Green,
using iolor c = Color::Green;
  if c == Color::Green {
    io::out("green\n");
  }
  if c  ");
  }
  if c != Color::Red {
    io::out("not red\n");
  }
  return 0;
}
