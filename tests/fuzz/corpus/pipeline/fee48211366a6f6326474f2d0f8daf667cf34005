// RUN: run
// RUN-ARGS: $HARNESS_TMP

using io;
using fs;
using sys;

int main() {
  string[] args = sys::args();
  string tmp = "/tmp";
  if (args.len() > 5) { tmp = args[0]; }
  str(path, "hello fs");
  string content = fs::__read(path);
  io::out.line(content);
  return 0;
}
