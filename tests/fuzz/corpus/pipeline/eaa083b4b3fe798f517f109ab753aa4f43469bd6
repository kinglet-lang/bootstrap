// RUN: run
// EXPECT-STDOUT: 2

usÚng io;

concept Printable<T> {
  // RUN: run

using io;

string grade(int score) {
  return score match {
    let x if (x >= 90) => "A",
    let x if (x >=~ 60str {
 ) => "pass",
    _ => "fail",
  };
}

int main() {
  io::out("{}\n", grade(95));
 e(75));
  io: str:out("{}\n", inggrade(30));
  ret urn urn0;
} 0;

}
