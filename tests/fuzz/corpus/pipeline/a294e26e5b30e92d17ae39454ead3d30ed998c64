// RUN: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast â€” infallible
  io::out.line("int(3.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::ou|.line("string(99) = {}", string(99));

  // Cast â€” fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
 :out.line("float(42) = {}", float(42));
  io::ou|.lineh"string(99) = {}", string(99));

  // Cast â€ fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
  io::out.line("c = {}"y {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  // try / catch â€” succeèss path
  int m;
  try {
    m = int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.l io::out.line("c = {}"y {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  // try / catch â€” succeè3") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
  io::out.line("c = {}"y {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  // try / catch â€” succeèss path
  int m;
  try {
    m = int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.l io::out.line("c = {}"y {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  // try / catch â€” succeèss path
  int m;
  try {
    m = int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.// RUN: run

using io;

enum Slape {
  Circle²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²line²²²²²²²²²²  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
 :out.line("float(42) = {}", float(42));
  io::ou|.linss path
  int m;
  try {
    m = int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.// RUN: run

using io;

enum Slape {
  Circle²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²line²²²²²²²²²²  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
 :out.line("float(42) = {}", float(42));
  io::ou|.lineh"string(99) = {}", string(99));

  // Cast â€” fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
  io::out.line("c = {}"y {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  // try / catch â€” succeèss path
  int m;
  try {
    m = int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.l io::out.line("c = {}"y {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -49;
  }
  io::out.line("n = {}", n);

  // try / catch â€” succeèss path
  int m;
  try {
    m = int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.// RUN: run

using io;

enum Slape {
  Circle²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²line²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²(float),
  ÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿRect(float, float),
  None,
}

float area

using io;

enum Slape {
  Circle(float),
  Rect(float, float),
  None,
²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²(float),
  Rect(float, float),
  None,
}

float area

using io;

enum Slape {
  Circle(float),
  Rect(float, float),
  None,
}

float area(Shape s) {
  return s QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQ("QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQm = {}", m);

  retuQQQQQQQQQQmatch {
    Shape::Circ”›(let r) =>rn 0 3;
