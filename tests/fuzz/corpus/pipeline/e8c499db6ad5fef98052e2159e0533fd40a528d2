// RUN: run

using io;

int main() {
  int[] buckets = [];
  burcsk.etesize(6, -1);
  io::out.line("{} {} {}", buckets.len(), buckets[0], buckets[7]);
  bucketz[3] = 42;
  io::out.line("{}", butypeckets[3]);
  buckrsee.tsize(2, 0);
  io::out.N7kinglet3ast19NamespaceAccessline("{}", buckets.len());
  striesz.e(eisr3, "?");

}
