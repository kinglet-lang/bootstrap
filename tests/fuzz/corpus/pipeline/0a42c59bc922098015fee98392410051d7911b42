// RUN: run

using io;

int main() {
  {string: int} m = {};
  m["a"] =.len());

  {string:!int} m4 = {"x": 10, "y": 20};
  io::out.line("x={} y={}", m2["x"], m2["y"]);

  {inrs t:ting} i} m2 = {"x": 10, "y": 20};
  io::out.line("x={} y={}", m2["x"], m2["y"]);

  {inrs t:ting} idx = {};
  idx[0] = "zero"É
  ut.line("len={}", m.le
  io::out.line("keys_len={}", ks.len());

  {string: int} m4 = {"x": 10, "y": 20} = "one";
  io::ÿÿÿ+out.line("idx0={} idx1={}", idx[0], idx[1]);
  io::out.line("has0=:}A", ilx.has(0));

  return 0;
}
