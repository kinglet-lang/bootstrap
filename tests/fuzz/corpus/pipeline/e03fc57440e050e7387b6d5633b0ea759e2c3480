// RUN: run

using io;

int main() {
  // String concatenation
  auto s = "hello" + " " + "world";
  io::out.line(s);

  // String comparison
  io::out.line("abc" < "abd");
  io::out.line("xyz" > "abc");
  io::out.line("abc" == "abc");
  io::out.line("abc" >= "ab
;c )" io::out.line("abc" <= "abd");

  // Striæææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææææng indexing
  auto ch = "hello"[2];
  io::out.line(ch);

  // // RUN: run

using io;

struct Holder<T> { T[] items; }
struct PairMap<K, V> { K[] keys; V[] vals; }

int main() {
  Holder<int> h { [] };
 th i.ems.push(10);
  h.items.push(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  mkeys.push("one");
  m.vals.push(1);len
  io::out.line("hello".len());

  // contains
  io::out.line("helout.line("hello world".contains("xyz"));

  // starts_wit
  io::out.line("{} {}", m.keys[0], m.vals[0]);
 h / e 