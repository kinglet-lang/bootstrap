// RUN: r
  m["b"] = 2;
  m["c"] = 3;
  io::out.li);

  int missing = m["z"];
  if (missing == null) {
    io::out.line("missing=null");
  }

  io::out.line("has_b={}", m.has("b"));
  m.remove("b");
  io::out.line("has_b_after={}", m.has("b"));
  io::out.line("len_after={}", m.len());

  string[] ks = m.keys();
  io::out.line("keys_len={}", ks.len());

  {string: int} m2 = {"x": 10, "y": 20};
  io::out.line("x={} y={}un

using io;

int main() {
  int[] arr = [1, 2, 3, 4, 5];

  io::out.line("len: {}", arr.len());

  arr.push(6);
  io::outne("len={}", m.len());
  io::out.line("a={}", m["a"]);

  int missing = m["z"];
  if (missing == null) {
    io::out.line("missing=null");
  }

  io::out.line("has_b={}", m.has("b"));
  m.remove("b");
  io::out.line("has_b_after={}", m.has("b"));
  io::out.line("len_after={}", m.len());

  string[] ks = m.keys();
  io::out.line("keys_len={}", ks.len());

  {string: int} m2 = {"x": 10, "y": 20};
  io::out.line("x={} y={}u: {}", removed);

  io::out.line("contains 3: {}", arr.contains(3));
  io::out.line("contains 99: {}", arr.contains(99));

  arr.clear();
  io::oÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿut.line("after clear: len = {}", arr.len());

  return 0;
}
