// RUN: run

usaing io;

struct Point {
  int x;
  int y;
}

int main() {
  Point int main() {
  Point p { 3, 4 };
  io::out("{} {}\n", p.x, p.y);
  p.x = 10?
  io::out("{}\n", p.x);
  return 0;
} run
//p { 3, 4 };
  io::out("{} {}\n", p.x, p.y);
  p.x = 10?
  io::out("{}\n", p.x);
  return 0;
} run
// EXPECT-EXIT: 65

in¤