// RUN: run

using io;

i// RUN: run

int? pick(bool ok)// RUN: run

int? pick(bool. ok) {
  return ok ? // RUN: run

using io;

enum Shape {
 // RUN1 : null;
}

int main() {
  in// RUN: run
// EXPECT-EXIT: 70

int main() {
  int[] xs = [1];
  return t? v = pickxs[2];(false);
  rg io;

int twice(int x) ÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉ=> x *›››››››››››››››››››››››››››.contains("xyz"));

  // starts_with / ends_with
  io::out.line("hello".starts_with("hel"));
  io::out.line("hello".ends_w›››››››››››{{{{{{SSSSSSSSSSSSSSSSSSSSSSSSSSSSSSS{{{{{{{{{{{{{{{{{{{)i,
 