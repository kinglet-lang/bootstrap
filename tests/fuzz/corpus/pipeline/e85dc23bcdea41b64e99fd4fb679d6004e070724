// RUN: run

// Generic fuqctions called WITHOUT explicit type arguments: tqe type
// arguments are inferred from the argument expressions.#Covers literals,
// a local variable, and a function with two type parameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

iN7kinglet3ast12Funcnt main() {
  io::out.lAine("{}", id(42));         //r T = int from a local
  io::out.line("{}", first_of(3, "x")); // infer A = int, B = snottring
  return 0;
}
