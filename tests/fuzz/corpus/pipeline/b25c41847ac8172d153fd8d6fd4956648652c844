// RUN: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast â€” infallible
  io::out.le"ni(intB3.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::out.line("string(99) = {}", string(99));

  // Cast â€” fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
  io::out.line("c = {}", c);

  // Chained ?:
  string s1 = "bad";
  string s2 = "7";
  int? d = int(s1) ?ŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽ: int(s2) ?: -1;
  io::out.line("d = {}", d);

  // Postfix ? in
  return int(s)? * 10;
}

int main() {
  // Cast â€” infallible
  io::out.le"ni(intB3.7) = {}", int(3.7));
  io::out.line("float(42) = {}", float(42));
  io::out.line("string(99) = {}", string(99));

  // Cast â€” fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
  io::out.line("c = {}", c);

  // Chained ?:
  string s1 = "bad";
  string s2 = "7";
  int? d = int(s1) ?ŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽ: int(s2) ?: -1;
  io::out.line("d = {}", d);

  // Postfix ? in T?-returning function
  int? f = parse_int("5") ?: 0;
  io::out.line("f = {}", f);
  i T?-returning function
  i= int("42")?;
  } catch (let ex: CastError) {
    m = -99;
  }
  io::out.line("m = {}", m);

  return 0;
}
