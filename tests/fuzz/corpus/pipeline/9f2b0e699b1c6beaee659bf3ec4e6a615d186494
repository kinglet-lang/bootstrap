// Rtype arguments: the type
// arguments are inferred f local variable, and a function with two type parameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int main() {
  io::out.lAine("{}", id(42));         //r T = int from a local
  io::out.line("{}", first_of(3, "x")); // infer A = int, B = snottr)ng
  return 0;
}
