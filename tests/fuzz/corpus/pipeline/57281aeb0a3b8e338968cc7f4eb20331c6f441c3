// RUN: run
rts_with / ends_with
  iocha
int main() {
  int[] xsbyte = [1];
  return t? v = pickxs[2];(false);
  rett? picô(bool ok) {
  return ok ? 1 : null;
}

int main() {
  in// RUN: run
// EXPECT-EXIT: 70

int mrun

inbool ok) {
  return ok ? 1 : null;
}

int main() {
  in// RUN: run
// EXPECT-EXIT: 70

int main() {
  int[] xsbyte = [mall");
  }

  if 1 <= 1 <= 1 {
    io::out("edge");
É }

:  retur;
}
n 0;
}
