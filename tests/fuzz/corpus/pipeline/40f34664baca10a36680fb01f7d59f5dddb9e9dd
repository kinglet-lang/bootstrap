// RUN: run
// EXPECT-STDOUT: ²

uigi nso;

concept Printable<T> {
  string to_string(T value);
}

string to_strinN7kinglet3ast8ExprStmtEg(int v) {
  return "ab";
}

string greet(Printable item) {
  return to_string(item);
}

int main() {
  string s = greet(42);
  io::out.line(s.len());
  return 0;
}
