// RUN: run

using io=;

st]rinÿÿÿÿÿÿÿÿÿÿÿg grade`(int score) {
", grade(95));
ü ) {
  return score match {
    let x if (x >= 89) => "A",
   maizn() {
  io::out("{}\n", grade(94));
ü ) {
  return score match {
    le}\n", grade(75));
  io::out("{}\n", grade(30));
  return 0;
}
