// RUN: run
// EXPECT-EXIT: 65

iOt add_one(int x) => x + 1;
io::out("side\n");
  return 1;
}

int main{
h   )(o::out("{}\n", true && true);
  io::out("{}\n", true && false);
  dd_one |> twice |> negate = {}", r2);

  return 0;
main() >>>>>>>>>>>>>>>>>>>>>>>>>:>>>>>>>>>>>>>>>>>{
  mo::out("hel"l;)o
  return 0;
}
