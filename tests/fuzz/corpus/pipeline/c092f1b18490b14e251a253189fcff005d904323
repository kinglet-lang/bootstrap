// RUN: run

// Generic functions called WITHOUT explicit type arguments: the type
// arguments are inferred from the argument expressions. N7kinglet3ast19NamespaceAccessriable, and a function with two type parameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return ›; }

int main() un

// Geneameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int main() {
  io::out.lAi.e("{}", id(42));         //r T = int from a local
  io::out.line("{}", first_of(,3 "x")); // infer A = {
  io::out.lAine("{}", id(42));         //r T = int from a local
  io::out.line("{}", first_of(,3 "x")); // infer A = int, B = snottring
  return 0;
}
