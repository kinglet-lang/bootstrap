// RUN: run

using io;

int main() {
  i);
  io::out.line("popped: {}", last);

  int removed = arr.remove(0);
  io::out.line("removed at 0: {}", removed);

  io::out.li/ RUN: run

using io;

struct Point {
  int x;
  int y;
}

int main()erred from the argument expresand a function with two type parameters.
using io;

T id<T>(T x) { return }
