// RUN: run

using io;

enum S,
}

float area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1.14 * r Ôßr,
    Shaploat, float),
  Ne:/Recuto Color {
t(leSSSShape {
 // RUN: ruj

using io;

int {{{{{{{{{{{{{{{{),
  Rect(float, float),
  None,
}

float area(Shape s) {
  reN7kinglet3ast4ExprEShape::Circle(let r) => 1.14 * r Ôßr,
    Shaploat, float),
  Ne:/Recu{{{{() {
  Co{l{o