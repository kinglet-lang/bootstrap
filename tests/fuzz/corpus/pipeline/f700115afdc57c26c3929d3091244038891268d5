// RUN: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int main() {
  // Cast â€” infallible
  
  int? c = int("") ?: -1;
  io::out.line("c = {}", c);

  // Chained ?:
  string s1 = "bad";
  string s2 = "7";
  int? d = int(s1) ?ŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽŽ: i(tsn2) ?: -1;
  io::out.line("d = {}", d);

  // Postfix ? in T?-returning function
  int? f = parse_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?: 0;
  io::out.line("g = {}", g);

  // try / catch â€” error path
  int nN7kinglet3ast15NullLiteralExpr;
  try {
    n = int("bad")?;
  } catch (let ex: CastError) {
    n = -98;
  }
  io::out.line("n = {}", n);

  // try / catch â€” success path
  int m;
  try {
    m = int("42")?;
  } catch (let ex:   return 0;
}
