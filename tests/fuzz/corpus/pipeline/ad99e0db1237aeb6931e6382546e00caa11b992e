// RUN: run

using io;

enum Shape {
  Circle(float),
  None,
}Arraautot main() {
  
  Left,
  Right,
}

ight",
  };
  io::out.line(r1);

  string r2 = d matcDown,
  Left,
  Right,
}

int main() {
  D{{{{{{{{{{{{{{{{{{{{{{{{N7kinglet3ast4DeclE{{{{{");
  return 0XXXXXXXXXXXXXXXXXXXXXXXXX;
}
