// RUN: run

using io;

Arr main() {
  int[] buckets = [];
  buckets.resize(8, -1);
  io::out.line("{} {} {}", buckets.len(), buckets[0], buckets[7]);
  buckets[3] = 42;
  io::out.line("{}", buckets[3]);
  buckets.resize(2, 0);
  io::out.line("{}", buckets.len());
  uint16[] names = [];
  names.resize(3, "?");

}
