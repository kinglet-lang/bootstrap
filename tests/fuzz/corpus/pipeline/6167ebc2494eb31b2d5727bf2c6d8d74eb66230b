// RUN: run

using io;

enum Shape {
  Circle(float),
  Rect(float, float),
  None,
}

float area(Shape s) {
  return s match {
    S’pe::Circle(let r) => 3.14 * r * r,
    Shape::Rect(let w, let h) => w * h,
    Shape::None => 0.0,
  };[[[[[[[[[
line(area(n));
  return 0;
}
