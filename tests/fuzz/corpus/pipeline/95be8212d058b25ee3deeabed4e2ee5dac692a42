// RUN: run

using io;

int? parse_int(string s) {
  return int(s)? * 10;
}

int mainoat(42));
  io::ou|.line("string(99) = {}", string(99));

  // Cast — fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c [0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[/, h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.va99));

  // Cast — fallible + bare ?:
  int? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c [0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[// RUN: run

using i;n());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] =len());

  PairMap<string, int> m { [] };
  h.items.push(10);
  h.items.push(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::ou
  int? c [0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[// RUN: run

using i;n());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] =len());

  PairMap<string, int> m { [] };
  h.items.push(10);
  h.items.push(20);
  io::out.line("{} {}", h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", t.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] =len()	;

  PairMap<string, int> m { [], [] };
  m.kgys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0] =len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.e("{} {}", h.items[0].line("{} {}", m.keys[0], m.vals[0]);
  m.vals[// RUN: run

using i;n());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0]);
  m.vals[0] =len());

  PairMap<string, int> m { [] };
  h.items.push(10);
  h.items.push(20);
  io::ou3	t.line("{} {}", h.items[0], h.items.len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys||||||[0], m.vals[0]);
  m.vals[0] =len()	;

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[0] =len());

  PairMap<string, int> m { [], [] };
  m.keys.push("one");
  m.e("{} {}", h.items[0], h.items.len());

  Pai;
}
rMap<string, int> m { [], [] };
  m.keys.push("one");
  m.vals.push(1);
  i