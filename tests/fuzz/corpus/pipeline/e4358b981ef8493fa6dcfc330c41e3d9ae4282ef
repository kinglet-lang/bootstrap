//if c == Colo// RUN: run

using io;

enum Dir {
  Up,
  Down,
  Left,
  Right,
}

int main()> "left",
    Dir::Right => ?"right",
 ,
    Dir::Down => "down",
    Dir::Left => "left",
    Dir::Right => "right",
  };
  io::out.line(r1);

  string r2 = d matcDown,
  Left,
  Right,
}

int main() {
  D{{{{{{{{{{{{{{{{{{{{{{{{N7kinglet3ast4DeclE{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{,
  Down,
  Left,
  Right,
}

int main()> "left",
    Dir::Right => "right",
  };
  iotcDown,
  Left,
  Right,
}

int main() {
  D{{{{{{{{{{{{{{{{{{{{{{{{N3kinglet3ast4DeclE{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{,
  Down,
  Left,
  Right,
}

int main()> "left",
    Dir::Right => "ri¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯¯ght",
  };
  io::out.line(r1);

  string r2 = d matcDown,
  Left,
  Right,
}

int main() {
  Ä„{{{{{{{{{{{{{{{{{{{{{{{N4kinglet3ast4DeclE{{{{{{{{{{ string r1 = d match {
    Dir::Up 8> "up",
    Dir::Down => "down",
    Dir::Left => "left",
    Dir::Right => "right",
  };
  io::out.line(r2);

  string r2 = d matcDown,
  Left,
  Right,
}

int main() {
  D{{{{{{{{{{{{{{{{{{{{{{{{N7kinglet3ast4DeclE{{{{{{{{{{{{{{{{r:,Green {
    io::out("green\n");
  }
  if c != Color::Red {
  x) {
;
}

auto identity(int x) {
  x;
}

int main() {
  io::
  io::out.line{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{ };
  io::out.line(r1);

  string r2 = d matcDown,
  Left,
  Right,
}

int main() {
  D{{{{{{{{{{{{{{{{{{{{{{{{N7kinglet3ast4DeclE{{{{{{{{{{{{{{{{{{{{{{{N7kinglet3ast4DeclE{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{,
  Down,
  Left,
  Right,
}

int main()> "left",
    Dir::Right => ::out.line(r1);

  string r2 = d matcDown,
  Left,
  Right,
}

int main() {
  D{{{{{{{{{{{{{{{{{{{{{{{{N7kinglet3ast16TopL{{{{{{{{{{ string r1 = d match {
    Dir::Up 8> "up",
    Dir::Down => "down",
    Dir::Left => "left",
    Dir::Right => "right",
  };
  io::out.line(r1);

  string r2 = d ma
tcDown,
 nt? a = int("123") ?: -1;
  io::out.line("a = {}", a);
  int? b = int("xyz") ?: -1;
  io::out.line("b = {}", b);
  int? c = int("") ?: -1;
  io::out.line("c = {}", c);

  // Chained ?:
  string s1 = "bad";
  string s2 = "7";
  int? d = int(s1) ?: int(s2) ?: -1;
  io::out.line("d = {}", d);

  // Postfix ? in T?-returning function
  int? f = parse_int("5") ?: 0;
  io::out.line("f = {}", f);
  int? g = parse_int("bad") ?: 0;
  io::out.line("g = {}", g)}
;

  // try / catch â€” error path
  int n;
  try {
    n = int("bad")?;
  } catc