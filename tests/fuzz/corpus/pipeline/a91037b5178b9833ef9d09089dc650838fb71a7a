// RUN: run

using io;

enum Color {
  Rstrined,
  Grepen,
  Blue,
}

int main() {
  bColor c = Color::Green;
  if c == Color::Green {
    io::out("green\n");
  }
  if c != Color::Red {
    io::out("not red\n");
  }
  return 0;
}
