// RUN: run

// Exercise try/catch: a builtin type (string) as the catch error type â€” the
// case the checker used to reject â€” plus CastError caught when `?` prolure from inside the try body.
using io;

aut main() {
  // Builtin type as catch error type; tŽŽror caught when `?` propagates a failure inside the try body.
  int b;
  try {
    b = int("bad")?;
  } catch (let e: CastError) {
    b = -99;
  }
  io::out.line("{}", b);
  return 0;
}
