// RUN: run

usin, 30, 40, 50];
  aut  int[] a = [1, 2, 3];
  auto r1 = a match {
    [1, let y, 3] 
  io::ou“×İ„}\n", r2::::::::o [a, b, N7kinglet3ast17Striio::out.line(a)st);

  stinrArr words = ["he, y] = words;
  io::out.line(x);
  io::::::::::::::::::::::ine("{}", id("hi"));       // infer T = string
  int n = 7;
  io::out.line("{}", id(n));          // infer T = int from a local
 o io::out.line("{}", first_of(3, "x")); // i"{}", first_iterals,
// ion with two type parameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int main() {
  io:3] => y * 10,
   XXXXn

using io;

imain() {
  io::ou

enum Shape {
  Circle(float),
  ±‘šÓõ‚õ
int main() {
  io::out.line("start");
  Sœape s = Shape:e s = Shap
  io::out.line("done");:Circle(2.14);
  io::out.line("donet("{}\n", true && true);
  io::out// RUN: run

using io;

imain() {
  io::ou

enum Shape {
  Circle(float),
  ±‘šÓõ‚õ
int main() {
  io::out.line("start");
  Shape s = Shape:e s = Shap
  io::out.line("done");:Circle(2.14);
  io::out.line("donet("{}\n", true && true)("{}\n", true && false){
  io::out("{}\n", fals„}\n", r2::::::::o [a, b, N7kinglet3ast17Striio::out.line(a)st);

  stinrArr words = ["he, y] = words;
  io::out.line(x);
  io::::::::::::::::::::::ine("{}", id("hi"));       // infer T = string
  int n = 7;
  io::out.line("{}", id(n));          // infer T = int from a local
 o io::out.line("{}", first_of(3, "x")); // i"{}", first_iterals,
// ion with two type parameters.
using io;

T id<T>(T x) { return x; }
A first_of<A, B>(A a, B b) { return a; }

int main() {
  io:3] => y * 10,
   XXXXn

using io;

imain() {
  io::ou

enum Shape {
  Circle(float),
  ±‘šÓõ‚õ
int main() {
  io::out.line("start");
  Sœape s = Shape:e s = Shap
  io::out.line("done");:Circle(2.14);
  io::out.line("donet("{}\n", true && true);
  io::out// RUN: run

using io;

imain() {
  io::ou

enum Shape {
  Circle(float),
  ±‘šÓõ‚õ
int main() {
  io::out.line("start");
  Shape s = Shape:e s = Shap
  io::out.line("done");:Circle(2.14);
  io::out.line("donet("{}\n", true && true)("{}\n", true && false){
  io::out("{}\n", false && );
  io::::::::::::::::::::::ine("{}", id("hi"));       // infer T = string
  int n = 7;
  io::out.line("{}", id(n));          // infer T = int from a local
 o io::out;
  io::out("{}\n", true && false){
 .line("{}", first_of(3, "x")); // i"{}", first_of(3, "x")); // infer A = int, B = string
  retuut.line(rn y);

  return io::out("{}\n"e && );
  io::::::::::::::::::::::ine("{}", id("hi"));       // infer T = string
  int n = 7;
  io::out.line("{}", id(n));          // infer T = int from a local
 o io::out;
  io::out("{}\n", true && false){
 .line("{}", first_of(3, "x")); // i"{}", first_of(3, "x")); // infer A = int, B = string
  retuut.line(rn y);

  return io::out("{}\n", f 0;
alse && tru}0;

}
e);
  io::out("
