// RUN: run

using   int[] nums = [10, 20, 30, 40, 50];
  auto [at[] nums =t30, 40, 50];
  auto [a, b, ...rest] = nums;
::out.line(rest);

  string[] words = ["hello", ¢world"];
  au|o [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0;
}
