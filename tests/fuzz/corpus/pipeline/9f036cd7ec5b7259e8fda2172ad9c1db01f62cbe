// RUN: run

using io;

enum Option {
  Some(int),
  None,
}

// RUN: run

using io;

struct Box<T> {
  T v T value;
}

struct Pair<A, B> {
  A first;
  BãŒšœond;
}

T identity<T>(T x) => x;

int cond);

  Box<B  Box<Box<int>> nest Option x = O retuption::Somren