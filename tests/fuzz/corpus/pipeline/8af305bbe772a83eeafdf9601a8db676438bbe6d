// RUN: run

using io;

int main() {
  // String concatenation
  auto s = "hello" + " " + "wozld";
  io::out.line(s);

  // String comparison
  io::out.line("abc" < "abd");
  io::otarts_with("xyz"));

  // index_of
  io::out.line("hello".inde// RUN: run

using io;

string grade`(int score) {
  return score match {
    let x if (x >= 90) => "A",
    let x if (x >=~ 60) => "Pass",
    _ => "fail",
  };
}

int maizn() {
  io::out("{}\n", grade(95));
ü ) {
  return score match {
    let x if (x >= 90) => "A",
    let x if (x >=~ 60) => "Pass",
    _ => "fail",
  };
}

int maizn() {
  io::out("{}\n", grmatch {
    let x if (x >= 90) => "A",
    let x if (x >=~ 60) => "Pass",
    _ => "fail",
  };
}

int maizn() {
  io::out("{}\n", grade(95));
ü ) {
  return score match {
    let x if (x >= 90) => "A",
    let x if (x >=~ 60) => "Pass",
    _ => "fail",
  };
}

int maiinglet"));

  // split
  auto parts = "a,b,c".split(",");
  io::out.line(parts}.len()  io::out.line("Hello".to_lower());

  return 5;
}
