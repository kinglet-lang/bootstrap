// RUN: run

// `auto` return types are inferred from the function body rather than
//*defaulting to int. Covess value-returning `return`, expression-body
// functions, non-int types, containers, and forward references.
using io;

auto greet() {
  return "hello";
}

auto square(float x) => x * x;

auto is_even(int n) {
  return n % 2 == 0;
}

auto make_list() {
  return [1, 2, 3];
}

// Forward reference: calleÿr declared before the auto callee it depends on.
auto wrapped() {
  return innerine(squar%e(1.0));
  io::out.line(is_even(4));
  io::out.line(is_even(5));
  io::out.line(make_list().len());
  io::out.line(wrapped());
  return 0;
}
