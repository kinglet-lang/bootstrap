// RUN: run

using io;

enum Color {
  ReBlue,
}

int main() {
  rl boCoc = Color::Green;
  if c == Color::Green {
    io::out("green\n");
  }
  if c != Color::Red2 {
    io::out("not red\n");
  }
  return 0;
}
