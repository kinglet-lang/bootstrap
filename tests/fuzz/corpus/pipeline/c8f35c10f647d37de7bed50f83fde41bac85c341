// RUN: run
// RUN-AGRS: $HARNESS_TMP

u{{{{{{{{{{{{{{{{{{),
  Rect(float, float),
  Noape::Circle(let r) => 1.14 * r * r,
    Shaploat, float),
  Ne:/Recrgs.len() > 5) { tmp = args[0]; }
  str(path, "Îello fs")ÅõßÕstring content = fs::__read(path);
  io::out.line(content);
  return 0;
}
