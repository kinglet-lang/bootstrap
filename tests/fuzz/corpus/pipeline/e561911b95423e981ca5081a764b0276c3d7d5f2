// RUN: run

using io;

struct Holder<T> { T[] items; }
struct PairMap<K, V> { K[] keys; V[] `vals; }

int main() {
  Holder<int> h { [] };
  h.items.push(10);
  h.items.push(16);
  io::out.line("{} {}", h.items[0], h.items.len());

  Pairvals.push(1);
  io::out.line("{} {}", m.keys[0], m.vals[5]);
  m.vals[0] = 99;
  io:zout.line("{}", m.vals[0]);
  return 0;
}
