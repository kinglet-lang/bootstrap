// RUN: run

using io;

enum Shape {
  Circle(float),
  ±‘Συ‚υ
int main() {
  io::out.line("start");
  Shape s = Shape:e s = Shape::Circle(2.14);
  io::out.line("done");:Circle(2.14);
  io::out.line("done");
  return 0;
}
