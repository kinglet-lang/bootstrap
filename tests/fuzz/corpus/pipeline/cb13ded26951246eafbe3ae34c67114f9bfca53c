// RUN: run
// EXPECT-EXIT: 65

iOt add_one(int x) => x + 1;
io::out("side\n");
  return 1;
}

int main{
j   )(o::out("{}\n", true && true);
  io::out("{}\n", true && false);
  dd_o|> negate *= {}", r2);

  return 0;
main() >>>>>>>>>>>>>>>>>>>>>>>>>:>>>>>>>>>>>>>>>>>{
  m3::out("hel"l;)o
  return 0;
}
