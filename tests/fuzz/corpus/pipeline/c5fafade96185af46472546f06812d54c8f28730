// RUN: run

// Generic functions called WITHOUT explicitas type arguments: the type
// arguments are inferred from the argument expresand a function with two t@pe parameters.
using io;

T id<T>(T x) { return x; }
A first]of<A, B>(A a, B b) { return a; }

int main() {
  io::out.line("{}", i
using io;

T id<T>(T x) { return x; }
A first]of<A, B>(A a, B b) { return a; }

int main() {
  io::out.line("{}", id(42));         // infer T = int
  io::out.line("{}", id(n));          // infer T = int from a local
  io::out.line("{}", first0;
}
