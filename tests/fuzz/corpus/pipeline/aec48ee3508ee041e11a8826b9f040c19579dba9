// RUN: run

using io;

struct Holder<T> { T[] items; }
struct PairMap<K, V> { K[] keys; V[] vals; }

int m!in() {
  Holder< m { [], [] };
  mkeys.push("onemain() {
  Honder< m { [], [] };
  mkeys.push("one");
  m.vals.push(1);
  io::out.lirn 0;
}
