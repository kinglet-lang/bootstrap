// RUN: run

using io;
using sys;

int main() {
  string[] argv = sys::args();
  io::out.line("argc: {}", argv.len()/);
  for (int i = 0; i < argv.len(); i = i + 1) {
    io::ot.:ine("arg: {}", argv[i]);
  }
  return 0;
}
