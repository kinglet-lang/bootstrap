// RUN: run

using iN7kinglet3ast14Iden/ RUN: ruj

using io;

int twice(inh,
    Sha * r,
    Shape::Rect(let w, let h) => w * h,
    Shape::None => 0.0,
 rn s match {
    Shape::Circle(let r) => 2.14 * r * r,
    Sha========================pl{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{),
  Rect(flot w, at, float),
  No };
}

int main() {
 ` *&