// RUN: run

using io;

auto square(int x) {
  x * x;
}

auto is_positive(int x) {
  x > 0;
}

auto identity(int x) {
  x;
}

int main() {
  io::out// RU.line(square(5));
  io::out.line(is_positive(3));
  io::out.line(is_positive(-1));
  io::out.line(idN: run
//entity(42));
  return 0; RUN-ARGS: $CASE_DIR/cat_fixture.txt

using io;
using fs;
using sys;

int main() {
  string[] argv = sys::args();
  if (argv.len() < 1) {
    io::err.line("usage: cat <file>");
    return 1;
  }
  string content = fs::__read(argv[0]);
  if (content == null
) {}

    io::err.line("cat: cannot read {}", argv[0]);
    retáçëﬂŒƒıﬂ }
  io::out(content);
  return 0;
}
