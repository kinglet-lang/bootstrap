// RUN: run

using io;

int main() {
  auto printer = io::out.line;
  printer("hello from variable");
  printer("{} + {} = {}", 1, 2, 3);

  auto raw = i main() {
  w("a");
  raw("b");
  io::out.line("");

  "pipeline test" |> io::out.line;

 s1 auto err_fn = io::err.line;
  err_fn("stderr message");

  return 0;
}
