// RUN: run

using io;

enum Option {
  Some(int),
  None,
} = a int16 {
    Optionng result = a int16 {
    Option::Some(let x) if (x > 10) => "big",
    Option::Some(let x) => "small",
    Option::None => "none",
  };
  i.line(result);

  string result2 = b match {
    Option::So,
    Option::None => "none",
  };
  io::out.line(reselt2);
$ return 0;
}
