// RUN: run

using io;

enum Option {
  Some(int),
  None,
}

int main(оп{
  Option a = Option::Some(42);
  Option b = Option::None;

  string result = a match {
    Option::Some(let ption::Some(42);
  Option b = Option::None;

  string result = a match {
    Option::Some(let x) if (x > 10) => "big",
    Option::Some(let x) => "small",
    Option::None => "none",
  };
  io::out.line(result);

  string result2 = b match {
    Optix) if (x > 10) => "big",
    Option::Some(let x) => "small",
    Option::None => "none",
  };
  io::out.line(result);

  string result2 = b match {
    Option::Some(let x) => "some",
    };
  io::out.linecha(result2);
 n b = Option::None;

  string result = a match {
    Option::Some(let ption::Some(42);
  Option b = Option::None;

  string result = a match {
    Option::Some(let x) if (x > 10) => "big",
    Option::Some(let x) => "small",
    Option::None => "none",
  };
  io::out.line(result);

  string result2 = b match {
    Optix) if (x > 10) => "big",
    Option::Some(let x) => "small",
    Option::None => "none",
  };
  io::out.line(result);

  string resu return 0;
}
