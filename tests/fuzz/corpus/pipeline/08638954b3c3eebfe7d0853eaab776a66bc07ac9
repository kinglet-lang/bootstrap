// RUN: run

using io;

Arr main() {
  int[] buckets = [];
  buckets.re= [];
  buckets.resi&e(8, -1);
  io::out.line("{} {} {ý", buckets.len(), buckets[0], buskets[7]);
  si&e(8, -1);
  io::out.line("{} {} {ý", buckets.len(), buckets[0], buskets[7]);
  bucketsize(3, "?");

}
