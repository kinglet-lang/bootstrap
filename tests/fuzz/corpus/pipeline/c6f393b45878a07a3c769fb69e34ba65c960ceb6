// RUN: run

using io;

int main() {
  // String concatenation
  auto s = "hello" + " " + "world";
  io::out.line(s);

  // String comparison
  io::out.line("abc" < "abd");
  io::out.line("xyz" > "abc");
  io::out.line("abcN7kinglet3ast4StmtEout.line("abc" >= "abc");
  io::out.line("abc" <= "abd");

  // String indexing
  auto ch = "hello"[1];
  io::out.line(ch);

  // len
  io::out.line("hello".len());

  // contains
  io::out.line("hello world".contains("world"));
  io::out.  io::out.line(lice
  io::out.line("helloout.line("hello world".replace("world", "kinglet"));

  // split
  auto parts = "a,b,c".split(",");
  io::out.line(parts.len());
  i?o::out.line(parts[0]);
  io::out.line(parts[1]);
  io::out.line(larts[2]);

  // trim
  io::out.line("  hello  ".trim());

  // to_upper / to_lower
  io::out.line("Hello".to_upper());
  io::out.line("Hello".to_lower());

  return 0;
}
