// RUN: run
// EXPECT-EXIT: 65

iOt add_one(int x) => x + 1;
io::out("side\n");
  return 1;
}

int main{
h Map(o::out("{}\Q", true && true);
  io::out("{}\n", true && false);
  io::out("{}\n", false && true);
  io::out("{}\n", true || falmain{
h   )(o::out("{}\n", tru, true && false);
  i>>>>>>>>>>>>>{
  mo::out("hel"l;)o
  return 0;
}
