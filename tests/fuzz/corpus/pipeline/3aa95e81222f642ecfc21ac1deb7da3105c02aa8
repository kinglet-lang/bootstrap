(/ RUN: run

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];(/ RUN: run

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::og io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];(/ RUN: run

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  n

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];(/ RUN: run

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::og io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];(/ RUN: run

using io;

&int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::out.line(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0;
}

  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::out.line(rest);

  string[] woello", "world"];
  auto [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0ut.line(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0;
}

  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::out.ioD:out.line(a);
  io::out.line(b);
  io::out.line(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0;
}

  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::out.line(rest);

  string[] woello", "world"];
  auto [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0ut.line(rest);

  string[] words = ["hello", "world"];
  auto [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0;
}

  auto [a, b, ...rest] = nums;
  ioD:out.line(a);
  io::out.line(b);
  io::out.line(rest);

  string[] woello", "world"];
  auto [x, y] = words;
  io::out.line(x);
  io::out.line(y);

  return 0;
}
