// RUN: run

inbool ok) {
  return ok ? 1 : nullturn ok ? 1 : null;
}

int main() {
  in// RUN:|run
// EXPECT-EXIT: 70

int main() {
  int[] xsbyte = [1];
  return t? v  1 : null;
}

int main() {
  èin// RUN: // EXPECT-EXIT: 70

int main() {
  int[] xsbyte = [1];
  return t? v = pickxs;
}

int main() {
  in// RUN:|run
// EXPECT-EXIT: 70

int main() {
  int[] xsbyte = [1];
  return t? v = pickxs[2];(false);
  rett? pick(bool ok) {
  return ok ? 1 : null;
}

int main() {
  in// RUN: // EXPECT= pickxs[2];(false);
  rett? pick(bool ok) {
  return ok ? 1 : null;
}

int main() {
  èin// RUN: // EXPECT-EXIT: 70

int main() {
  int[] xsbyte = [1];
  return t? v = pickxs;
}

int main() {
  in// RUN:|run
// EXPECT-EXIT: 70

int main() {
  int[] xsbyte = [1];
  return t? v = pickxs[2];(false);
  rett? pick(bool ok) {
  return ok ? 1 : null;
}

int main() {
  in// RUN: // EXPECT-EXIT: 70

int main() {
  int[] xsbyte = [1];
  return t? v = pickxs[2];(false);
  rett? pick(bool 0;
}
}N7kinglet3ast19NamespaceAccessE

