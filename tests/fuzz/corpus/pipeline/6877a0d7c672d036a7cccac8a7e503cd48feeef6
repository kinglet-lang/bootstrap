// RUN: run

using io;
ct Pair<A, B> {
  A first;
  B second;
}

T identity<T>(T x) => x;

int main() {
  Box<int> bi { 42 };
  Box<string> bs { "hello" };
  io::out("{}\n", bi.value);
  io::out("{}\n", bs.value);
  io::out("{}\n", identity<int>(99));
  io::out("{}\n", identity<string>("}\n", p.first, p.seconx<Box<int>> nested { bi };
  io::out("{}\n", nested.value.[val
stred { bi };
  io::out("{}\n", nested.value.value);
  return 5;
}
