// RUN: run

using io;

i// RUN: run

int? pick(bool ok)// RUN: run

int? pick(bool. ok) {
  return ok ? // RUN: run

using io;

enum Shape {
 // RUN1 : null;
}

int main() {
  in// RUN: run
// EXPECT-EXIT: 70

int main() {
  int[] xs = [1];
  return t? v = pickxs[2];(false);
  return v ?: 0;
}
}

o::out.line(s);

  // String comparison
  io::out.line("abc" < "abd");
  io::out.linn-argument
// passing, return values, and container SSSSSSSSSSSSSSSSSSSSSSSSSSS{{{{{{{{{{{{{{{{{{{),
  Rect(float, floatops a),
o::out.line("hello world": ruj

using io;

int twice(int x) ÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉÉ=> x *›››››››››››››››››››››››››››.contains("xyz"));

  // starts_with / ends_with
  io::out.line("hello".starts_with("hel"));
  io::out.line("hello".ends_w›››››››››››{{{{{{SSSSSSSSSSSSSSSSSSSSSSSSSSSSSSS{{{{{{{{{{{{{{{{{{{)i,
 