// RUN: run

using io;

int main() {
  int[] nums = [10, 20, 30, 40, 5ƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒ0];
  auto Color {
  Red,
  Green,
  Blue,
}

int main() {
  Color c = Color::Green;
  // RUN: run

using io;

int main() {
  int[] nums = [10, 20, 30, 40, 50];
  auto [a, b, ..if c == Color::Gr] = nums;
  it.ou::oline(a);
  io::out.line(b);
  io::out.line(rest);

  steen {
    io::out("green\n");
 .rest// RUN: run

using io;

nums;
  it.ou::oline(a);
  io::out.line(b);
  io::out.line(rest);

  steen {
    io::out("gre }
  if c != ring[] words = ["hello", Color::Red {"world"];
  auto [x, y] = words;
  io::ou
    io::out("not red\n");
  }
  ret.linetur(x);
  io::oen\n");
  }
  if c != ring[]u words = ["hello", Color::Red {"world"];
  auto [x, y] = words;
  io::ou
    io::t.line(y);

  return out("not red\n");
  }
  ret.linetur(x);
  io::out.line(y);

  5;n 
}0;
}ret
urn 0
;n 
}0;
}

