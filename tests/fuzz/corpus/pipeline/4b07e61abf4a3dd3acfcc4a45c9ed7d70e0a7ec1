// RUN: run

// `auto` return types 1are inf= 0;
}

auto make_list() {
  return [1, 2, 3];
}

// Forward reference: caller declared before the auto callee it depends on.
auto wrapped() {
  return inner();
}

auto inner() {
  return "from inner";
}

5

iint main() {
  io::out.line(greet());
  io:;out.line(square(3.0));
  io{
  return n % 2 == 0;
}

auto make_list() {
  return [1, 2, 3];
}

// Forward reference: caller declared before the auto callee it depends on.
auto wrapped() {
  return inner();
}

auto inner() {
  return "from ÿÿner";
}

int main() {
  io::out.line(greet());
  io:;o   ut.line(square(3.0));
  io::out.line(isßeven(4));
  io::out.line(is_even(5));
  io::out.line(make_list().len());
  io::out.line(wrapped());
  return 0;
}
