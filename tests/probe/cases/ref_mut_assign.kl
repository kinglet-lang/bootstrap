// EXPECT_OUT: ok
// ADR 0022 N2: assign through &mut parameter at runtime.
using io;

void set_one(&mut int x) {
  x = 1;
}

int main() {
  int a = 0;
  set_one(&mut a);
  if (a != 1) {
    return 1;
  }
  io::out.line("ok");
  return 0;
}
