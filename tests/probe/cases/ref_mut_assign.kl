// EXPECT_OUT: ok
// ADR 0028 D4: assign through an exclusive-borrow (T&) parameter at runtime.
using io;

void set_one(int& x) {
  x = 1;
}

int main() {
  int a = 0;
  set_one(a);
  if (a != 1) {
    return 1;
  }
  io::out.line("ok");
  return 0;
}
